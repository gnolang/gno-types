// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               v6.33.4
// source: gno/tm2/bft/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../google/protobuf/any.js";
import { Timestamp } from "../../../google/protobuf/timestamp.js";
import { SimpleProof } from "../crypto/merkle.js";
import { ResponseBeginBlock, ResponseDeliverTx, ResponseEndBlock, ValidatorUpdate } from "./abci.js";

/** messages */
export interface Proposal {
  type: number;
  height: bigint;
  round: bigint;
  polRound: bigint;
  blockId?: BlockID | undefined;
  timestamp?: Timestamp | undefined;
  signature: Uint8Array;
}

export interface Block {
  header?: Header | undefined;
  data?: Data | undefined;
  lastCommit?: Commit | undefined;
}

export interface Header {
  version: string;
  chainId: string;
  height: bigint;
  time?: Timestamp | undefined;
  numTxs: bigint;
  totalTxs: bigint;
  appVersion: string;
  lastBlockId?: BlockID | undefined;
  lastCommitHash: Uint8Array;
  dataHash: Uint8Array;
  validatorsHash: Uint8Array;
  nextValidatorsHash: Uint8Array;
  consensusHash: Uint8Array;
  appHash: Uint8Array;
  lastResultsHash: Uint8Array;
  proposerAddress: string;
}

export interface Data {
  txs: Uint8Array[];
}

export interface Commit {
  blockId?: BlockID | undefined;
  precommits: CommitSig[];
}

export interface BlockID {
  hash: Uint8Array;
  partsHeader?: PartSetHeader | undefined;
}

export interface CommitSig {
  type: number;
  height: bigint;
  round: bigint;
  blockId?: BlockID | undefined;
  timestamp?: Timestamp | undefined;
  validatorAddress: string;
  validatorIndex: bigint;
  signature: Uint8Array;
}

export interface Vote {
  type: number;
  height: bigint;
  round: bigint;
  blockId?: BlockID | undefined;
  timestamp?: Timestamp | undefined;
  validatorAddress: string;
  validatorIndex: bigint;
  signature: Uint8Array;
}

export interface Part {
  index: bigint;
  bytes: Uint8Array;
  proof?: SimpleProof | undefined;
}

export interface PartSet {
}

export interface PartSetHeader {
  total: bigint;
  hash: Uint8Array;
}

export interface CanonicalBlockID {
  hash: Uint8Array;
  partsHeader?: CanonicalPartSetHeader | undefined;
}

export interface CanonicalPartSetHeader {
  total: number;
  hash: Uint8Array;
}

export interface CanonicalProposal {
  type: number;
  height: bigint;
  round: bigint;
  polRound: bigint;
  blockId?: CanonicalBlockID | undefined;
  timestamp?: Timestamp | undefined;
  chainId: string;
}

export interface CanonicalVote {
  type: number;
  height: bigint;
  round: bigint;
  blockId?: CanonicalBlockID | undefined;
  timestamp?: Timestamp | undefined;
  chainId: string;
}

export interface Validator {
  address: string;
  pubKey?: Any | undefined;
  votingPower: bigint;
  proposerPriority: bigint;
}

export interface ValidatorSet {
  validators: Validator[];
  proposer?: Validator | undefined;
}

export interface EventNewBlock {
  block?: Block | undefined;
  resultBeginBlock?: ResponseBeginBlock | undefined;
  resultEndBlock?: ResponseEndBlock | undefined;
}

export interface EventNewBlockHeader {
  header?: Header | undefined;
  resultBeginBlock?: ResponseBeginBlock | undefined;
  resultEndBlock?: ResponseEndBlock | undefined;
}

export interface EventTx {
  result?: TxResult | undefined;
}

export interface EventVote {
  vote?: Vote | undefined;
}

export interface EventValidatorSetUpdates {
  validatorUpdates: ValidatorUpdate[];
}

export interface DuplicateVoteEvidence {
  pubKey?: Any | undefined;
  voteA?: Vote | undefined;
  voteB?: Vote | undefined;
}

export interface MockGoodEvidence {
  height: bigint;
  address: string;
}

export interface MockRandomGoodEvidence {
  mockGoodEvidence?: MockGoodEvidence | undefined;
}

export interface MockBadEvidence {
  mockGoodEvidence?: MockGoodEvidence | undefined;
}

export interface TxResult {
  height: bigint;
  index: number;
  tx: Uint8Array;
  response?: ResponseDeliverTx | undefined;
}

export interface MockAppState {
  accountOwner: string;
}

export interface VoteSet {
}

export interface TYPESBytesList {
  Value: Uint8Array[];
}

function createBaseProposal(): Proposal {
  return {
    type: 0,
    height: 0n,
    round: 0n,
    polRound: 0n,
    blockId: undefined,
    timestamp: undefined,
    signature: new Uint8Array(0),
  };
}

export const Proposal: MessageFns<Proposal> = {
  encode(message: Proposal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).uint32(message.type);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(16).sint64(message.height);
    }
    if (message.round !== 0n) {
      if (BigInt.asIntN(64, message.round) !== message.round) {
        throw new globalThis.Error("value provided for field message.round of type sint64 too large");
      }
      writer.uint32(24).sint64(message.round);
    }
    if (message.polRound !== 0n) {
      if (BigInt.asIntN(64, message.polRound) !== message.polRound) {
        throw new globalThis.Error("value provided for field message.polRound of type sint64 too large");
      }
      writer.uint32(32).sint64(message.polRound);
    }
    if (message.blockId !== undefined) {
      BlockID.encode(message.blockId, writer.uint32(42).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(50).fork()).join();
    }
    if (message.signature.length !== 0) {
      writer.uint32(58).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Proposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseProposal();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.round = reader.sint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.polRound = reader.sint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.blockId = BlockID.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.timestamp = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.signature = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Proposal {
    return {
      type: isSet(object.Type)
        ? globalThis.Number(object.Type)
        : isSet(object.type)
        ? globalThis.Number(object.type)
        : 0,
      height: isSet(object.height) ? BigInt(object.height) : 0n,
      round: isSet(object.round) ? BigInt(object.round) : 0n,
      polRound: isSet(object.polRound)
        ? BigInt(object.polRound)
        : isSet(object.pol_round)
        ? BigInt(object.pol_round)
        : 0n,
      blockId: isSet(object.blockId)
        ? BlockID.fromJSON(object.blockId)
        : isSet(object.block_id)
        ? BlockID.fromJSON(object.block_id)
        : undefined,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: Proposal): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.Type = Math.round(message.type);
    }
    if (message.height !== undefined) {
      obj.height = message.height.toString();
    }
    if (message.round !== undefined) {
      obj.round = message.round.toString();
    }
    if (message.polRound !== undefined) {
      obj.polRound = message.polRound.toString();
    }
    if (message.blockId !== undefined) {
      obj.blockId = BlockID.toJSON(message.blockId);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = fromTimestamp(message.timestamp).toISOString();
    }
    if (message.signature !== undefined) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Proposal>, I>>(base?: I): Proposal {
    return Proposal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Proposal>, I>>(object: I): Proposal {
    const message = createBaseProposal();
    message.type = object.type ?? 0;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.round = (object.round !== undefined && object.round !== null) ? BigInt(object.round) : 0n;
    message.polRound = (object.polRound !== undefined && object.polRound !== null) ? BigInt(object.polRound) : 0n;
    message.blockId = (object.blockId !== undefined && object.blockId !== null)
      ? BlockID.fromPartial(object.blockId)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseBlock(): Block {
  return { header: undefined, data: undefined, lastCommit: undefined };
}

export const Block: MessageFns<Block> = {
  encode(message: Block, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      Header.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      Data.encode(message.data, writer.uint32(18).fork()).join();
    }
    if (message.lastCommit !== undefined) {
      Commit.encode(message.lastCommit, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Block {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBlock();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.header = Header.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.data = Data.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.lastCommit = Commit.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Block {
    return {
      header: isSet(object.header) ? Header.fromJSON(object.header) : undefined,
      data: isSet(object.data) ? Data.fromJSON(object.data) : undefined,
      lastCommit: isSet(object.lastCommit)
        ? Commit.fromJSON(object.lastCommit)
        : isSet(object.last_commit)
        ? Commit.fromJSON(object.last_commit)
        : undefined,
    };
  },

  toJSON(message: Block): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = Header.toJSON(message.header);
    }
    if (message.data !== undefined) {
      obj.data = Data.toJSON(message.data);
    }
    if (message.lastCommit !== undefined) {
      obj.lastCommit = Commit.toJSON(message.lastCommit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Block>, I>>(base?: I): Block {
    return Block.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Block>, I>>(object: I): Block {
    const message = createBaseBlock();
    message.header = (object.header !== undefined && object.header !== null)
      ? Header.fromPartial(object.header)
      : undefined;
    message.data = (object.data !== undefined && object.data !== null) ? Data.fromPartial(object.data) : undefined;
    message.lastCommit = (object.lastCommit !== undefined && object.lastCommit !== null)
      ? Commit.fromPartial(object.lastCommit)
      : undefined;
    return message;
  },
};

function createBaseHeader(): Header {
  return {
    version: "",
    chainId: "",
    height: 0n,
    time: undefined,
    numTxs: 0n,
    totalTxs: 0n,
    appVersion: "",
    lastBlockId: undefined,
    lastCommitHash: new Uint8Array(0),
    dataHash: new Uint8Array(0),
    validatorsHash: new Uint8Array(0),
    nextValidatorsHash: new Uint8Array(0),
    consensusHash: new Uint8Array(0),
    appHash: new Uint8Array(0),
    lastResultsHash: new Uint8Array(0),
    proposerAddress: "",
  };
}

export const Header: MessageFns<Header> = {
  encode(message: Header, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.chainId !== "") {
      writer.uint32(18).string(message.chainId);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(24).sint64(message.height);
    }
    if (message.time !== undefined) {
      Timestamp.encode(message.time, writer.uint32(34).fork()).join();
    }
    if (message.numTxs !== 0n) {
      if (BigInt.asIntN(64, message.numTxs) !== message.numTxs) {
        throw new globalThis.Error("value provided for field message.numTxs of type sint64 too large");
      }
      writer.uint32(40).sint64(message.numTxs);
    }
    if (message.totalTxs !== 0n) {
      if (BigInt.asIntN(64, message.totalTxs) !== message.totalTxs) {
        throw new globalThis.Error("value provided for field message.totalTxs of type sint64 too large");
      }
      writer.uint32(48).sint64(message.totalTxs);
    }
    if (message.appVersion !== "") {
      writer.uint32(58).string(message.appVersion);
    }
    if (message.lastBlockId !== undefined) {
      BlockID.encode(message.lastBlockId, writer.uint32(66).fork()).join();
    }
    if (message.lastCommitHash.length !== 0) {
      writer.uint32(74).bytes(message.lastCommitHash);
    }
    if (message.dataHash.length !== 0) {
      writer.uint32(82).bytes(message.dataHash);
    }
    if (message.validatorsHash.length !== 0) {
      writer.uint32(90).bytes(message.validatorsHash);
    }
    if (message.nextValidatorsHash.length !== 0) {
      writer.uint32(98).bytes(message.nextValidatorsHash);
    }
    if (message.consensusHash.length !== 0) {
      writer.uint32(106).bytes(message.consensusHash);
    }
    if (message.appHash.length !== 0) {
      writer.uint32(114).bytes(message.appHash);
    }
    if (message.lastResultsHash.length !== 0) {
      writer.uint32(122).bytes(message.lastResultsHash);
    }
    if (message.proposerAddress !== "") {
      writer.uint32(130).string(message.proposerAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Header {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHeader();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.version = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.chainId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.time = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.numTxs = reader.sint64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.totalTxs = reader.sint64() as bigint;
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.appVersion = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.lastBlockId = BlockID.decode(reader, reader.uint32());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.lastCommitHash = reader.bytes();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.dataHash = reader.bytes();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.validatorsHash = reader.bytes();
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.nextValidatorsHash = reader.bytes();
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.consensusHash = reader.bytes();
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.appHash = reader.bytes();
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.lastResultsHash = reader.bytes();
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.proposerAddress = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Header {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      chainId: isSet(object.chainId)
        ? globalThis.String(object.chainId)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      height: isSet(object.height) ? BigInt(object.height) : 0n,
      time: isSet(object.time) ? fromJsonTimestamp(object.time) : undefined,
      numTxs: isSet(object.numTxs) ? BigInt(object.numTxs) : isSet(object.num_txs) ? BigInt(object.num_txs) : 0n,
      totalTxs: isSet(object.totalTxs)
        ? BigInt(object.totalTxs)
        : isSet(object.total_txs)
        ? BigInt(object.total_txs)
        : 0n,
      appVersion: isSet(object.appVersion)
        ? globalThis.String(object.appVersion)
        : isSet(object.app_version)
        ? globalThis.String(object.app_version)
        : "",
      lastBlockId: isSet(object.lastBlockId)
        ? BlockID.fromJSON(object.lastBlockId)
        : isSet(object.last_block_id)
        ? BlockID.fromJSON(object.last_block_id)
        : undefined,
      lastCommitHash: isSet(object.lastCommitHash)
        ? bytesFromBase64(object.lastCommitHash)
        : isSet(object.last_commit_hash)
        ? bytesFromBase64(object.last_commit_hash)
        : new Uint8Array(0),
      dataHash: isSet(object.dataHash)
        ? bytesFromBase64(object.dataHash)
        : isSet(object.data_hash)
        ? bytesFromBase64(object.data_hash)
        : new Uint8Array(0),
      validatorsHash: isSet(object.validatorsHash)
        ? bytesFromBase64(object.validatorsHash)
        : isSet(object.validators_hash)
        ? bytesFromBase64(object.validators_hash)
        : new Uint8Array(0),
      nextValidatorsHash: isSet(object.nextValidatorsHash)
        ? bytesFromBase64(object.nextValidatorsHash)
        : isSet(object.next_validators_hash)
        ? bytesFromBase64(object.next_validators_hash)
        : new Uint8Array(0),
      consensusHash: isSet(object.consensusHash)
        ? bytesFromBase64(object.consensusHash)
        : isSet(object.consensus_hash)
        ? bytesFromBase64(object.consensus_hash)
        : new Uint8Array(0),
      appHash: isSet(object.appHash)
        ? bytesFromBase64(object.appHash)
        : isSet(object.app_hash)
        ? bytesFromBase64(object.app_hash)
        : new Uint8Array(0),
      lastResultsHash: isSet(object.lastResultsHash)
        ? bytesFromBase64(object.lastResultsHash)
        : isSet(object.last_results_hash)
        ? bytesFromBase64(object.last_results_hash)
        : new Uint8Array(0),
      proposerAddress: isSet(object.proposerAddress)
        ? globalThis.String(object.proposerAddress)
        : isSet(object.proposer_address)
        ? globalThis.String(object.proposer_address)
        : "",
    };
  },

  toJSON(message: Header): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = message.version;
    }
    if (message.chainId !== undefined) {
      obj.chainId = message.chainId;
    }
    if (message.height !== undefined) {
      obj.height = message.height.toString();
    }
    if (message.time !== undefined) {
      obj.time = fromTimestamp(message.time).toISOString();
    }
    if (message.numTxs !== undefined) {
      obj.numTxs = message.numTxs.toString();
    }
    if (message.totalTxs !== undefined) {
      obj.totalTxs = message.totalTxs.toString();
    }
    if (message.appVersion !== undefined) {
      obj.appVersion = message.appVersion;
    }
    if (message.lastBlockId !== undefined) {
      obj.lastBlockId = BlockID.toJSON(message.lastBlockId);
    }
    if (message.lastCommitHash !== undefined) {
      obj.lastCommitHash = base64FromBytes(message.lastCommitHash);
    }
    if (message.dataHash !== undefined) {
      obj.dataHash = base64FromBytes(message.dataHash);
    }
    if (message.validatorsHash !== undefined) {
      obj.validatorsHash = base64FromBytes(message.validatorsHash);
    }
    if (message.nextValidatorsHash !== undefined) {
      obj.nextValidatorsHash = base64FromBytes(message.nextValidatorsHash);
    }
    if (message.consensusHash !== undefined) {
      obj.consensusHash = base64FromBytes(message.consensusHash);
    }
    if (message.appHash !== undefined) {
      obj.appHash = base64FromBytes(message.appHash);
    }
    if (message.lastResultsHash !== undefined) {
      obj.lastResultsHash = base64FromBytes(message.lastResultsHash);
    }
    if (message.proposerAddress !== undefined) {
      obj.proposerAddress = message.proposerAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Header>, I>>(base?: I): Header {
    return Header.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Header>, I>>(object: I): Header {
    const message = createBaseHeader();
    message.version = object.version ?? "";
    message.chainId = object.chainId ?? "";
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.time = (object.time !== undefined && object.time !== null) ? Timestamp.fromPartial(object.time) : undefined;
    message.numTxs = (object.numTxs !== undefined && object.numTxs !== null) ? BigInt(object.numTxs) : 0n;
    message.totalTxs = (object.totalTxs !== undefined && object.totalTxs !== null) ? BigInt(object.totalTxs) : 0n;
    message.appVersion = object.appVersion ?? "";
    message.lastBlockId = (object.lastBlockId !== undefined && object.lastBlockId !== null)
      ? BlockID.fromPartial(object.lastBlockId)
      : undefined;
    message.lastCommitHash = object.lastCommitHash ?? new Uint8Array(0);
    message.dataHash = object.dataHash ?? new Uint8Array(0);
    message.validatorsHash = object.validatorsHash ?? new Uint8Array(0);
    message.nextValidatorsHash = object.nextValidatorsHash ?? new Uint8Array(0);
    message.consensusHash = object.consensusHash ?? new Uint8Array(0);
    message.appHash = object.appHash ?? new Uint8Array(0);
    message.lastResultsHash = object.lastResultsHash ?? new Uint8Array(0);
    message.proposerAddress = object.proposerAddress ?? "";
    return message;
  },
};

function createBaseData(): Data {
  return { txs: [] };
}

export const Data: MessageFns<Data> = {
  encode(message: Data, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.txs) {
      writer.uint32(10).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Data {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseData();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.txs.push(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Data {
    return { txs: globalThis.Array.isArray(object?.txs) ? object.txs.map((e: any) => bytesFromBase64(e)) : [] };
  },

  toJSON(message: Data): unknown {
    const obj: any = {};
    if (message.txs?.length) {
      obj.txs = message.txs.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Data>, I>>(base?: I): Data {
    return Data.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Data>, I>>(object: I): Data {
    const message = createBaseData();
    message.txs = object.txs?.map((e) => e) || [];
    return message;
  },
};

function createBaseCommit(): Commit {
  return { blockId: undefined, precommits: [] };
}

export const Commit: MessageFns<Commit> = {
  encode(message: Commit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.blockId !== undefined) {
      BlockID.encode(message.blockId, writer.uint32(10).fork()).join();
    }
    for (const v of message.precommits) {
      CommitSig.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Commit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCommit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.blockId = BlockID.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.precommits.push(CommitSig.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Commit {
    return {
      blockId: isSet(object.blockId)
        ? BlockID.fromJSON(object.blockId)
        : isSet(object.block_id)
        ? BlockID.fromJSON(object.block_id)
        : undefined,
      precommits: globalThis.Array.isArray(object?.precommits)
        ? object.precommits.map((e: any) => CommitSig.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Commit): unknown {
    const obj: any = {};
    if (message.blockId !== undefined) {
      obj.blockId = BlockID.toJSON(message.blockId);
    }
    if (message.precommits?.length) {
      obj.precommits = message.precommits.map((e) => CommitSig.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Commit>, I>>(base?: I): Commit {
    return Commit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Commit>, I>>(object: I): Commit {
    const message = createBaseCommit();
    message.blockId = (object.blockId !== undefined && object.blockId !== null)
      ? BlockID.fromPartial(object.blockId)
      : undefined;
    message.precommits = object.precommits?.map((e) => CommitSig.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBlockID(): BlockID {
  return { hash: new Uint8Array(0), partsHeader: undefined };
}

export const BlockID: MessageFns<BlockID> = {
  encode(message: BlockID, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash.length !== 0) {
      writer.uint32(10).bytes(message.hash);
    }
    if (message.partsHeader !== undefined) {
      PartSetHeader.encode(message.partsHeader, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BlockID {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBlockID();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.hash = reader.bytes();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.partsHeader = PartSetHeader.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BlockID {
    return {
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : new Uint8Array(0),
      partsHeader: isSet(object.parts)
        ? PartSetHeader.fromJSON(object.parts)
        : isSet(object.parts_header)
        ? PartSetHeader.fromJSON(object.parts_header)
        : undefined,
    };
  },

  toJSON(message: BlockID): unknown {
    const obj: any = {};
    if (message.hash !== undefined) {
      obj.hash = base64FromBytes(message.hash);
    }
    if (message.partsHeader !== undefined) {
      obj.parts = PartSetHeader.toJSON(message.partsHeader);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlockID>, I>>(base?: I): BlockID {
    return BlockID.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlockID>, I>>(object: I): BlockID {
    const message = createBaseBlockID();
    message.hash = object.hash ?? new Uint8Array(0);
    message.partsHeader = (object.partsHeader !== undefined && object.partsHeader !== null)
      ? PartSetHeader.fromPartial(object.partsHeader)
      : undefined;
    return message;
  },
};

function createBaseCommitSig(): CommitSig {
  return {
    type: 0,
    height: 0n,
    round: 0n,
    blockId: undefined,
    timestamp: undefined,
    validatorAddress: "",
    validatorIndex: 0n,
    signature: new Uint8Array(0),
  };
}

export const CommitSig: MessageFns<CommitSig> = {
  encode(message: CommitSig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).uint32(message.type);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(16).sint64(message.height);
    }
    if (message.round !== 0n) {
      if (BigInt.asIntN(64, message.round) !== message.round) {
        throw new globalThis.Error("value provided for field message.round of type sint64 too large");
      }
      writer.uint32(24).sint64(message.round);
    }
    if (message.blockId !== undefined) {
      BlockID.encode(message.blockId, writer.uint32(34).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(42).fork()).join();
    }
    if (message.validatorAddress !== "") {
      writer.uint32(50).string(message.validatorAddress);
    }
    if (message.validatorIndex !== 0n) {
      if (BigInt.asIntN(64, message.validatorIndex) !== message.validatorIndex) {
        throw new globalThis.Error("value provided for field message.validatorIndex of type sint64 too large");
      }
      writer.uint32(56).sint64(message.validatorIndex);
    }
    if (message.signature.length !== 0) {
      writer.uint32(66).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommitSig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCommitSig();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.round = reader.sint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.blockId = BlockID.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.timestamp = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.validatorAddress = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.validatorIndex = reader.sint64() as bigint;
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.signature = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CommitSig {
    return {
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      height: isSet(object.height) ? BigInt(object.height) : 0n,
      round: isSet(object.round) ? BigInt(object.round) : 0n,
      blockId: isSet(object.blockId)
        ? BlockID.fromJSON(object.blockId)
        : isSet(object.block_id)
        ? BlockID.fromJSON(object.block_id)
        : undefined,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      validatorAddress: isSet(object.validatorAddress)
        ? globalThis.String(object.validatorAddress)
        : isSet(object.validator_address)
        ? globalThis.String(object.validator_address)
        : "",
      validatorIndex: isSet(object.validatorIndex)
        ? BigInt(object.validatorIndex)
        : isSet(object.validator_index)
        ? BigInt(object.validator_index)
        : 0n,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: CommitSig): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.type = Math.round(message.type);
    }
    if (message.height !== undefined) {
      obj.height = message.height.toString();
    }
    if (message.round !== undefined) {
      obj.round = message.round.toString();
    }
    if (message.blockId !== undefined) {
      obj.blockId = BlockID.toJSON(message.blockId);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = fromTimestamp(message.timestamp).toISOString();
    }
    if (message.validatorAddress !== undefined) {
      obj.validatorAddress = message.validatorAddress;
    }
    if (message.validatorIndex !== undefined) {
      obj.validatorIndex = message.validatorIndex.toString();
    }
    if (message.signature !== undefined) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommitSig>, I>>(base?: I): CommitSig {
    return CommitSig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommitSig>, I>>(object: I): CommitSig {
    const message = createBaseCommitSig();
    message.type = object.type ?? 0;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.round = (object.round !== undefined && object.round !== null) ? BigInt(object.round) : 0n;
    message.blockId = (object.blockId !== undefined && object.blockId !== null)
      ? BlockID.fromPartial(object.blockId)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.validatorAddress = object.validatorAddress ?? "";
    message.validatorIndex = (object.validatorIndex !== undefined && object.validatorIndex !== null)
      ? BigInt(object.validatorIndex)
      : 0n;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseVote(): Vote {
  return {
    type: 0,
    height: 0n,
    round: 0n,
    blockId: undefined,
    timestamp: undefined,
    validatorAddress: "",
    validatorIndex: 0n,
    signature: new Uint8Array(0),
  };
}

export const Vote: MessageFns<Vote> = {
  encode(message: Vote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).uint32(message.type);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(16).sint64(message.height);
    }
    if (message.round !== 0n) {
      if (BigInt.asIntN(64, message.round) !== message.round) {
        throw new globalThis.Error("value provided for field message.round of type sint64 too large");
      }
      writer.uint32(24).sint64(message.round);
    }
    if (message.blockId !== undefined) {
      BlockID.encode(message.blockId, writer.uint32(34).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(42).fork()).join();
    }
    if (message.validatorAddress !== "") {
      writer.uint32(50).string(message.validatorAddress);
    }
    if (message.validatorIndex !== 0n) {
      if (BigInt.asIntN(64, message.validatorIndex) !== message.validatorIndex) {
        throw new globalThis.Error("value provided for field message.validatorIndex of type sint64 too large");
      }
      writer.uint32(56).sint64(message.validatorIndex);
    }
    if (message.signature.length !== 0) {
      writer.uint32(66).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Vote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVote();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.round = reader.sint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.blockId = BlockID.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.timestamp = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.validatorAddress = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.validatorIndex = reader.sint64() as bigint;
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.signature = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Vote {
    return {
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      height: isSet(object.height) ? BigInt(object.height) : 0n,
      round: isSet(object.round) ? BigInt(object.round) : 0n,
      blockId: isSet(object.blockId)
        ? BlockID.fromJSON(object.blockId)
        : isSet(object.block_id)
        ? BlockID.fromJSON(object.block_id)
        : undefined,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      validatorAddress: isSet(object.validatorAddress)
        ? globalThis.String(object.validatorAddress)
        : isSet(object.validator_address)
        ? globalThis.String(object.validator_address)
        : "",
      validatorIndex: isSet(object.validatorIndex)
        ? BigInt(object.validatorIndex)
        : isSet(object.validator_index)
        ? BigInt(object.validator_index)
        : 0n,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: Vote): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.type = Math.round(message.type);
    }
    if (message.height !== undefined) {
      obj.height = message.height.toString();
    }
    if (message.round !== undefined) {
      obj.round = message.round.toString();
    }
    if (message.blockId !== undefined) {
      obj.blockId = BlockID.toJSON(message.blockId);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = fromTimestamp(message.timestamp).toISOString();
    }
    if (message.validatorAddress !== undefined) {
      obj.validatorAddress = message.validatorAddress;
    }
    if (message.validatorIndex !== undefined) {
      obj.validatorIndex = message.validatorIndex.toString();
    }
    if (message.signature !== undefined) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Vote>, I>>(base?: I): Vote {
    return Vote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Vote>, I>>(object: I): Vote {
    const message = createBaseVote();
    message.type = object.type ?? 0;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.round = (object.round !== undefined && object.round !== null) ? BigInt(object.round) : 0n;
    message.blockId = (object.blockId !== undefined && object.blockId !== null)
      ? BlockID.fromPartial(object.blockId)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.validatorAddress = object.validatorAddress ?? "";
    message.validatorIndex = (object.validatorIndex !== undefined && object.validatorIndex !== null)
      ? BigInt(object.validatorIndex)
      : 0n;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBasePart(): Part {
  return { index: 0n, bytes: new Uint8Array(0), proof: undefined };
}

export const Part: MessageFns<Part> = {
  encode(message: Part, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0n) {
      if (BigInt.asIntN(64, message.index) !== message.index) {
        throw new globalThis.Error("value provided for field message.index of type sint64 too large");
      }
      writer.uint32(8).sint64(message.index);
    }
    if (message.bytes.length !== 0) {
      writer.uint32(18).bytes(message.bytes);
    }
    if (message.proof !== undefined) {
      SimpleProof.encode(message.proof, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Part {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePart();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.index = reader.sint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.bytes = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.proof = SimpleProof.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Part {
    return {
      index: isSet(object.index) ? BigInt(object.index) : 0n,
      bytes: isSet(object.bytes) ? bytesFromBase64(object.bytes) : new Uint8Array(0),
      proof: isSet(object.proof) ? SimpleProof.fromJSON(object.proof) : undefined,
    };
  },

  toJSON(message: Part): unknown {
    const obj: any = {};
    if (message.index !== undefined) {
      obj.index = message.index.toString();
    }
    if (message.bytes !== undefined) {
      obj.bytes = base64FromBytes(message.bytes);
    }
    if (message.proof !== undefined) {
      obj.proof = SimpleProof.toJSON(message.proof);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Part>, I>>(base?: I): Part {
    return Part.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Part>, I>>(object: I): Part {
    const message = createBasePart();
    message.index = (object.index !== undefined && object.index !== null) ? BigInt(object.index) : 0n;
    message.bytes = object.bytes ?? new Uint8Array(0);
    message.proof = (object.proof !== undefined && object.proof !== null)
      ? SimpleProof.fromPartial(object.proof)
      : undefined;
    return message;
  },
};

function createBasePartSet(): PartSet {
  return {};
}

export const PartSet: MessageFns<PartSet> = {
  encode(_: PartSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PartSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePartSet();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): PartSet {
    return {};
  },

  toJSON(_: PartSet): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PartSet>, I>>(base?: I): PartSet {
    return PartSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartSet>, I>>(_: I): PartSet {
    const message = createBasePartSet();
    return message;
  },
};

function createBasePartSetHeader(): PartSetHeader {
  return { total: 0n, hash: new Uint8Array(0) };
}

export const PartSetHeader: MessageFns<PartSetHeader> = {
  encode(message: PartSetHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0n) {
      if (BigInt.asIntN(64, message.total) !== message.total) {
        throw new globalThis.Error("value provided for field message.total of type sint64 too large");
      }
      writer.uint32(8).sint64(message.total);
    }
    if (message.hash.length !== 0) {
      writer.uint32(18).bytes(message.hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PartSetHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePartSetHeader();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.total = reader.sint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.hash = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PartSetHeader {
    return {
      total: isSet(object.total) ? BigInt(object.total) : 0n,
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : new Uint8Array(0),
    };
  },

  toJSON(message: PartSetHeader): unknown {
    const obj: any = {};
    if (message.total !== undefined) {
      obj.total = message.total.toString();
    }
    if (message.hash !== undefined) {
      obj.hash = base64FromBytes(message.hash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartSetHeader>, I>>(base?: I): PartSetHeader {
    return PartSetHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartSetHeader>, I>>(object: I): PartSetHeader {
    const message = createBasePartSetHeader();
    message.total = (object.total !== undefined && object.total !== null) ? BigInt(object.total) : 0n;
    message.hash = object.hash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCanonicalBlockID(): CanonicalBlockID {
  return { hash: new Uint8Array(0), partsHeader: undefined };
}

export const CanonicalBlockID: MessageFns<CanonicalBlockID> = {
  encode(message: CanonicalBlockID, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash.length !== 0) {
      writer.uint32(10).bytes(message.hash);
    }
    if (message.partsHeader !== undefined) {
      CanonicalPartSetHeader.encode(message.partsHeader, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CanonicalBlockID {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCanonicalBlockID();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.hash = reader.bytes();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.partsHeader = CanonicalPartSetHeader.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CanonicalBlockID {
    return {
      hash: isSet(object.Hash)
        ? bytesFromBase64(object.Hash)
        : isSet(object.hash)
        ? bytesFromBase64(object.hash)
        : new Uint8Array(0),
      partsHeader: isSet(object.PartsHeader)
        ? CanonicalPartSetHeader.fromJSON(object.PartsHeader)
        : isSet(object.parts_header)
        ? CanonicalPartSetHeader.fromJSON(object.parts_header)
        : undefined,
    };
  },

  toJSON(message: CanonicalBlockID): unknown {
    const obj: any = {};
    if (message.hash !== undefined) {
      obj.Hash = base64FromBytes(message.hash);
    }
    if (message.partsHeader !== undefined) {
      obj.PartsHeader = CanonicalPartSetHeader.toJSON(message.partsHeader);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CanonicalBlockID>, I>>(base?: I): CanonicalBlockID {
    return CanonicalBlockID.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CanonicalBlockID>, I>>(object: I): CanonicalBlockID {
    const message = createBaseCanonicalBlockID();
    message.hash = object.hash ?? new Uint8Array(0);
    message.partsHeader = (object.partsHeader !== undefined && object.partsHeader !== null)
      ? CanonicalPartSetHeader.fromPartial(object.partsHeader)
      : undefined;
    return message;
  },
};

function createBaseCanonicalPartSetHeader(): CanonicalPartSetHeader {
  return { total: 0, hash: new Uint8Array(0) };
}

export const CanonicalPartSetHeader: MessageFns<CanonicalPartSetHeader> = {
  encode(message: CanonicalPartSetHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0) {
      writer.uint32(8).uint32(message.total);
    }
    if (message.hash.length !== 0) {
      writer.uint32(18).bytes(message.hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CanonicalPartSetHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCanonicalPartSetHeader();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.total = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.hash = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CanonicalPartSetHeader {
    return {
      total: isSet(object.Total)
        ? globalThis.Number(object.Total)
        : isSet(object.total)
        ? globalThis.Number(object.total)
        : 0,
      hash: isSet(object.Hash)
        ? bytesFromBase64(object.Hash)
        : isSet(object.hash)
        ? bytesFromBase64(object.hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: CanonicalPartSetHeader): unknown {
    const obj: any = {};
    if (message.total !== undefined) {
      obj.Total = Math.round(message.total);
    }
    if (message.hash !== undefined) {
      obj.Hash = base64FromBytes(message.hash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CanonicalPartSetHeader>, I>>(base?: I): CanonicalPartSetHeader {
    return CanonicalPartSetHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CanonicalPartSetHeader>, I>>(object: I): CanonicalPartSetHeader {
    const message = createBaseCanonicalPartSetHeader();
    message.total = object.total ?? 0;
    message.hash = object.hash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCanonicalProposal(): CanonicalProposal {
  return { type: 0, height: 0n, round: 0n, polRound: 0n, blockId: undefined, timestamp: undefined, chainId: "" };
}

export const CanonicalProposal: MessageFns<CanonicalProposal> = {
  encode(message: CanonicalProposal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).uint32(message.type);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sfixed64 too large");
      }
      writer.uint32(17).sfixed64(message.height);
    }
    if (message.round !== 0n) {
      if (BigInt.asIntN(64, message.round) !== message.round) {
        throw new globalThis.Error("value provided for field message.round of type sfixed64 too large");
      }
      writer.uint32(25).sfixed64(message.round);
    }
    if (message.polRound !== 0n) {
      if (BigInt.asIntN(64, message.polRound) !== message.polRound) {
        throw new globalThis.Error("value provided for field message.polRound of type int64 too large");
      }
      writer.uint32(32).int64(message.polRound);
    }
    if (message.blockId !== undefined) {
      CanonicalBlockID.encode(message.blockId, writer.uint32(42).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(50).fork()).join();
    }
    if (message.chainId !== "") {
      writer.uint32(58).string(message.chainId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CanonicalProposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCanonicalProposal();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.height = reader.sfixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.round = reader.sfixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.polRound = reader.int64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.blockId = CanonicalBlockID.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.timestamp = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.chainId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CanonicalProposal {
    return {
      type: isSet(object.Type)
        ? globalThis.Number(object.Type)
        : isSet(object.type)
        ? globalThis.Number(object.type)
        : 0,
      height: isSet(object.Height) ? BigInt(object.Height) : isSet(object.height) ? BigInt(object.height) : 0n,
      round: isSet(object.Round) ? BigInt(object.Round) : isSet(object.round) ? BigInt(object.round) : 0n,
      polRound: isSet(object.POLRound)
        ? BigInt(object.POLRound)
        : isSet(object.pol_round)
        ? BigInt(object.pol_round)
        : 0n,
      blockId: isSet(object.BlockID)
        ? CanonicalBlockID.fromJSON(object.BlockID)
        : isSet(object.block_id)
        ? CanonicalBlockID.fromJSON(object.block_id)
        : undefined,
      timestamp: isSet(object.Timestamp)
        ? fromJsonTimestamp(object.Timestamp)
        : isSet(object.timestamp)
        ? fromJsonTimestamp(object.timestamp)
        : undefined,
      chainId: isSet(object.ChainID)
        ? globalThis.String(object.ChainID)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
    };
  },

  toJSON(message: CanonicalProposal): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.Type = Math.round(message.type);
    }
    if (message.height !== undefined) {
      obj.Height = message.height.toString();
    }
    if (message.round !== undefined) {
      obj.Round = message.round.toString();
    }
    if (message.polRound !== undefined) {
      obj.POLRound = message.polRound.toString();
    }
    if (message.blockId !== undefined) {
      obj.BlockID = CanonicalBlockID.toJSON(message.blockId);
    }
    if (message.timestamp !== undefined) {
      obj.Timestamp = fromTimestamp(message.timestamp).toISOString();
    }
    if (message.chainId !== undefined) {
      obj.ChainID = message.chainId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CanonicalProposal>, I>>(base?: I): CanonicalProposal {
    return CanonicalProposal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CanonicalProposal>, I>>(object: I): CanonicalProposal {
    const message = createBaseCanonicalProposal();
    message.type = object.type ?? 0;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.round = (object.round !== undefined && object.round !== null) ? BigInt(object.round) : 0n;
    message.polRound = (object.polRound !== undefined && object.polRound !== null) ? BigInt(object.polRound) : 0n;
    message.blockId = (object.blockId !== undefined && object.blockId !== null)
      ? CanonicalBlockID.fromPartial(object.blockId)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.chainId = object.chainId ?? "";
    return message;
  },
};

function createBaseCanonicalVote(): CanonicalVote {
  return { type: 0, height: 0n, round: 0n, blockId: undefined, timestamp: undefined, chainId: "" };
}

export const CanonicalVote: MessageFns<CanonicalVote> = {
  encode(message: CanonicalVote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).uint32(message.type);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sfixed64 too large");
      }
      writer.uint32(17).sfixed64(message.height);
    }
    if (message.round !== 0n) {
      if (BigInt.asIntN(64, message.round) !== message.round) {
        throw new globalThis.Error("value provided for field message.round of type sfixed64 too large");
      }
      writer.uint32(25).sfixed64(message.round);
    }
    if (message.blockId !== undefined) {
      CanonicalBlockID.encode(message.blockId, writer.uint32(34).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(42).fork()).join();
    }
    if (message.chainId !== "") {
      writer.uint32(50).string(message.chainId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CanonicalVote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCanonicalVote();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.height = reader.sfixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.round = reader.sfixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.blockId = CanonicalBlockID.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.timestamp = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.chainId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CanonicalVote {
    return {
      type: isSet(object.Type)
        ? globalThis.Number(object.Type)
        : isSet(object.type)
        ? globalThis.Number(object.type)
        : 0,
      height: isSet(object.Height) ? BigInt(object.Height) : isSet(object.height) ? BigInt(object.height) : 0n,
      round: isSet(object.Round) ? BigInt(object.Round) : isSet(object.round) ? BigInt(object.round) : 0n,
      blockId: isSet(object.BlockID)
        ? CanonicalBlockID.fromJSON(object.BlockID)
        : isSet(object.block_id)
        ? CanonicalBlockID.fromJSON(object.block_id)
        : undefined,
      timestamp: isSet(object.Timestamp)
        ? fromJsonTimestamp(object.Timestamp)
        : isSet(object.timestamp)
        ? fromJsonTimestamp(object.timestamp)
        : undefined,
      chainId: isSet(object.ChainID)
        ? globalThis.String(object.ChainID)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
    };
  },

  toJSON(message: CanonicalVote): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.Type = Math.round(message.type);
    }
    if (message.height !== undefined) {
      obj.Height = message.height.toString();
    }
    if (message.round !== undefined) {
      obj.Round = message.round.toString();
    }
    if (message.blockId !== undefined) {
      obj.BlockID = CanonicalBlockID.toJSON(message.blockId);
    }
    if (message.timestamp !== undefined) {
      obj.Timestamp = fromTimestamp(message.timestamp).toISOString();
    }
    if (message.chainId !== undefined) {
      obj.ChainID = message.chainId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CanonicalVote>, I>>(base?: I): CanonicalVote {
    return CanonicalVote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CanonicalVote>, I>>(object: I): CanonicalVote {
    const message = createBaseCanonicalVote();
    message.type = object.type ?? 0;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.round = (object.round !== undefined && object.round !== null) ? BigInt(object.round) : 0n;
    message.blockId = (object.blockId !== undefined && object.blockId !== null)
      ? CanonicalBlockID.fromPartial(object.blockId)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.chainId = object.chainId ?? "";
    return message;
  },
};

function createBaseValidator(): Validator {
  return { address: "", pubKey: undefined, votingPower: 0n, proposerPriority: 0n };
}

export const Validator: MessageFns<Validator> = {
  encode(message: Validator, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.pubKey !== undefined) {
      Any.encode(message.pubKey, writer.uint32(18).fork()).join();
    }
    if (message.votingPower !== 0n) {
      if (BigInt.asIntN(64, message.votingPower) !== message.votingPower) {
        throw new globalThis.Error("value provided for field message.votingPower of type sint64 too large");
      }
      writer.uint32(24).sint64(message.votingPower);
    }
    if (message.proposerPriority !== 0n) {
      if (BigInt.asIntN(64, message.proposerPriority) !== message.proposerPriority) {
        throw new globalThis.Error("value provided for field message.proposerPriority of type sint64 too large");
      }
      writer.uint32(32).sint64(message.proposerPriority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Validator {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseValidator();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.address = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.pubKey = Any.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.votingPower = reader.sint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.proposerPriority = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Validator {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      pubKey: isSet(object.pubKey)
        ? Any.fromJSON(object.pubKey)
        : isSet(object.pub_key)
        ? Any.fromJSON(object.pub_key)
        : undefined,
      votingPower: isSet(object.votingPower)
        ? BigInt(object.votingPower)
        : isSet(object.voting_power)
        ? BigInt(object.voting_power)
        : 0n,
      proposerPriority: isSet(object.proposerPriority)
        ? BigInt(object.proposerPriority)
        : isSet(object.proposer_priority)
        ? BigInt(object.proposer_priority)
        : 0n,
    };
  },

  toJSON(message: Validator): unknown {
    const obj: any = {};
    if (message.address !== undefined) {
      obj.address = message.address;
    }
    if (message.pubKey !== undefined) {
      obj.pubKey = Any.toJSON(message.pubKey);
    }
    if (message.votingPower !== undefined) {
      obj.votingPower = message.votingPower.toString();
    }
    if (message.proposerPriority !== undefined) {
      obj.proposerPriority = message.proposerPriority.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Validator>, I>>(base?: I): Validator {
    return Validator.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Validator>, I>>(object: I): Validator {
    const message = createBaseValidator();
    message.address = object.address ?? "";
    message.pubKey = (object.pubKey !== undefined && object.pubKey !== null)
      ? Any.fromPartial(object.pubKey)
      : undefined;
    message.votingPower = (object.votingPower !== undefined && object.votingPower !== null)
      ? BigInt(object.votingPower)
      : 0n;
    message.proposerPriority = (object.proposerPriority !== undefined && object.proposerPriority !== null)
      ? BigInt(object.proposerPriority)
      : 0n;
    return message;
  },
};

function createBaseValidatorSet(): ValidatorSet {
  return { validators: [], proposer: undefined };
}

export const ValidatorSet: MessageFns<ValidatorSet> = {
  encode(message: ValidatorSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.validators) {
      Validator.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.proposer !== undefined) {
      Validator.encode(message.proposer, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidatorSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseValidatorSet();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.validators.push(Validator.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.proposer = Validator.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ValidatorSet {
    return {
      validators: globalThis.Array.isArray(object?.validators)
        ? object.validators.map((e: any) => Validator.fromJSON(e))
        : [],
      proposer: isSet(object.proposer) ? Validator.fromJSON(object.proposer) : undefined,
    };
  },

  toJSON(message: ValidatorSet): unknown {
    const obj: any = {};
    if (message.validators?.length) {
      obj.validators = message.validators.map((e) => Validator.toJSON(e));
    }
    if (message.proposer !== undefined) {
      obj.proposer = Validator.toJSON(message.proposer);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidatorSet>, I>>(base?: I): ValidatorSet {
    return ValidatorSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidatorSet>, I>>(object: I): ValidatorSet {
    const message = createBaseValidatorSet();
    message.validators = object.validators?.map((e) => Validator.fromPartial(e)) || [];
    message.proposer = (object.proposer !== undefined && object.proposer !== null)
      ? Validator.fromPartial(object.proposer)
      : undefined;
    return message;
  },
};

function createBaseEventNewBlock(): EventNewBlock {
  return { block: undefined, resultBeginBlock: undefined, resultEndBlock: undefined };
}

export const EventNewBlock: MessageFns<EventNewBlock> = {
  encode(message: EventNewBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.block !== undefined) {
      Block.encode(message.block, writer.uint32(10).fork()).join();
    }
    if (message.resultBeginBlock !== undefined) {
      ResponseBeginBlock.encode(message.resultBeginBlock, writer.uint32(18).fork()).join();
    }
    if (message.resultEndBlock !== undefined) {
      ResponseEndBlock.encode(message.resultEndBlock, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventNewBlock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventNewBlock();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.block = Block.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.resultBeginBlock = ResponseBeginBlock.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.resultEndBlock = ResponseEndBlock.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventNewBlock {
    return {
      block: isSet(object.block) ? Block.fromJSON(object.block) : undefined,
      resultBeginBlock: isSet(object.resultBeginBlock)
        ? ResponseBeginBlock.fromJSON(object.resultBeginBlock)
        : isSet(object.result_begin_block)
        ? ResponseBeginBlock.fromJSON(object.result_begin_block)
        : undefined,
      resultEndBlock: isSet(object.resultEndBlock)
        ? ResponseEndBlock.fromJSON(object.resultEndBlock)
        : isSet(object.result_end_block)
        ? ResponseEndBlock.fromJSON(object.result_end_block)
        : undefined,
    };
  },

  toJSON(message: EventNewBlock): unknown {
    const obj: any = {};
    if (message.block !== undefined) {
      obj.block = Block.toJSON(message.block);
    }
    if (message.resultBeginBlock !== undefined) {
      obj.resultBeginBlock = ResponseBeginBlock.toJSON(message.resultBeginBlock);
    }
    if (message.resultEndBlock !== undefined) {
      obj.resultEndBlock = ResponseEndBlock.toJSON(message.resultEndBlock);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventNewBlock>, I>>(base?: I): EventNewBlock {
    return EventNewBlock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventNewBlock>, I>>(object: I): EventNewBlock {
    const message = createBaseEventNewBlock();
    message.block = (object.block !== undefined && object.block !== null) ? Block.fromPartial(object.block) : undefined;
    message.resultBeginBlock = (object.resultBeginBlock !== undefined && object.resultBeginBlock !== null)
      ? ResponseBeginBlock.fromPartial(object.resultBeginBlock)
      : undefined;
    message.resultEndBlock = (object.resultEndBlock !== undefined && object.resultEndBlock !== null)
      ? ResponseEndBlock.fromPartial(object.resultEndBlock)
      : undefined;
    return message;
  },
};

function createBaseEventNewBlockHeader(): EventNewBlockHeader {
  return { header: undefined, resultBeginBlock: undefined, resultEndBlock: undefined };
}

export const EventNewBlockHeader: MessageFns<EventNewBlockHeader> = {
  encode(message: EventNewBlockHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      Header.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.resultBeginBlock !== undefined) {
      ResponseBeginBlock.encode(message.resultBeginBlock, writer.uint32(18).fork()).join();
    }
    if (message.resultEndBlock !== undefined) {
      ResponseEndBlock.encode(message.resultEndBlock, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventNewBlockHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventNewBlockHeader();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.header = Header.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.resultBeginBlock = ResponseBeginBlock.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.resultEndBlock = ResponseEndBlock.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventNewBlockHeader {
    return {
      header: isSet(object.header) ? Header.fromJSON(object.header) : undefined,
      resultBeginBlock: isSet(object.resultBeginBlock)
        ? ResponseBeginBlock.fromJSON(object.resultBeginBlock)
        : isSet(object.result_begin_block)
        ? ResponseBeginBlock.fromJSON(object.result_begin_block)
        : undefined,
      resultEndBlock: isSet(object.resultEndBlock)
        ? ResponseEndBlock.fromJSON(object.resultEndBlock)
        : isSet(object.result_end_block)
        ? ResponseEndBlock.fromJSON(object.result_end_block)
        : undefined,
    };
  },

  toJSON(message: EventNewBlockHeader): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = Header.toJSON(message.header);
    }
    if (message.resultBeginBlock !== undefined) {
      obj.resultBeginBlock = ResponseBeginBlock.toJSON(message.resultBeginBlock);
    }
    if (message.resultEndBlock !== undefined) {
      obj.resultEndBlock = ResponseEndBlock.toJSON(message.resultEndBlock);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventNewBlockHeader>, I>>(base?: I): EventNewBlockHeader {
    return EventNewBlockHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventNewBlockHeader>, I>>(object: I): EventNewBlockHeader {
    const message = createBaseEventNewBlockHeader();
    message.header = (object.header !== undefined && object.header !== null)
      ? Header.fromPartial(object.header)
      : undefined;
    message.resultBeginBlock = (object.resultBeginBlock !== undefined && object.resultBeginBlock !== null)
      ? ResponseBeginBlock.fromPartial(object.resultBeginBlock)
      : undefined;
    message.resultEndBlock = (object.resultEndBlock !== undefined && object.resultEndBlock !== null)
      ? ResponseEndBlock.fromPartial(object.resultEndBlock)
      : undefined;
    return message;
  },
};

function createBaseEventTx(): EventTx {
  return { result: undefined };
}

export const EventTx: MessageFns<EventTx> = {
  encode(message: EventTx, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== undefined) {
      TxResult.encode(message.result, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventTx {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventTx();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.result = TxResult.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventTx {
    return { result: isSet(object.result) ? TxResult.fromJSON(object.result) : undefined };
  },

  toJSON(message: EventTx): unknown {
    const obj: any = {};
    if (message.result !== undefined) {
      obj.result = TxResult.toJSON(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventTx>, I>>(base?: I): EventTx {
    return EventTx.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventTx>, I>>(object: I): EventTx {
    const message = createBaseEventTx();
    message.result = (object.result !== undefined && object.result !== null)
      ? TxResult.fromPartial(object.result)
      : undefined;
    return message;
  },
};

function createBaseEventVote(): EventVote {
  return { vote: undefined };
}

export const EventVote: MessageFns<EventVote> = {
  encode(message: EventVote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vote !== undefined) {
      Vote.encode(message.vote, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventVote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventVote();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.vote = Vote.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventVote {
    return { vote: isSet(object.vote) ? Vote.fromJSON(object.vote) : undefined };
  },

  toJSON(message: EventVote): unknown {
    const obj: any = {};
    if (message.vote !== undefined) {
      obj.vote = Vote.toJSON(message.vote);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventVote>, I>>(base?: I): EventVote {
    return EventVote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventVote>, I>>(object: I): EventVote {
    const message = createBaseEventVote();
    message.vote = (object.vote !== undefined && object.vote !== null) ? Vote.fromPartial(object.vote) : undefined;
    return message;
  },
};

function createBaseEventValidatorSetUpdates(): EventValidatorSetUpdates {
  return { validatorUpdates: [] };
}

export const EventValidatorSetUpdates: MessageFns<EventValidatorSetUpdates> = {
  encode(message: EventValidatorSetUpdates, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.validatorUpdates) {
      ValidatorUpdate.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventValidatorSetUpdates {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventValidatorSetUpdates();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.validatorUpdates.push(ValidatorUpdate.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventValidatorSetUpdates {
    return {
      validatorUpdates: globalThis.Array.isArray(object?.validatorUpdates)
        ? object.validatorUpdates.map((e: any) => ValidatorUpdate.fromJSON(e))
        : globalThis.Array.isArray(object?.validator_updates)
        ? object.validator_updates.map((e: any) => ValidatorUpdate.fromJSON(e))
        : [],
    };
  },

  toJSON(message: EventValidatorSetUpdates): unknown {
    const obj: any = {};
    if (message.validatorUpdates?.length) {
      obj.validatorUpdates = message.validatorUpdates.map((e) => ValidatorUpdate.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventValidatorSetUpdates>, I>>(base?: I): EventValidatorSetUpdates {
    return EventValidatorSetUpdates.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventValidatorSetUpdates>, I>>(object: I): EventValidatorSetUpdates {
    const message = createBaseEventValidatorSetUpdates();
    message.validatorUpdates = object.validatorUpdates?.map((e) => ValidatorUpdate.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDuplicateVoteEvidence(): DuplicateVoteEvidence {
  return { pubKey: undefined, voteA: undefined, voteB: undefined };
}

export const DuplicateVoteEvidence: MessageFns<DuplicateVoteEvidence> = {
  encode(message: DuplicateVoteEvidence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pubKey !== undefined) {
      Any.encode(message.pubKey, writer.uint32(10).fork()).join();
    }
    if (message.voteA !== undefined) {
      Vote.encode(message.voteA, writer.uint32(18).fork()).join();
    }
    if (message.voteB !== undefined) {
      Vote.encode(message.voteB, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DuplicateVoteEvidence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDuplicateVoteEvidence();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.pubKey = Any.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.voteA = Vote.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.voteB = Vote.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DuplicateVoteEvidence {
    return {
      pubKey: isSet(object.PubKey)
        ? Any.fromJSON(object.PubKey)
        : isSet(object.pub_key)
        ? Any.fromJSON(object.pub_key)
        : undefined,
      voteA: isSet(object.VoteA)
        ? Vote.fromJSON(object.VoteA)
        : isSet(object.vote_a)
        ? Vote.fromJSON(object.vote_a)
        : undefined,
      voteB: isSet(object.VoteB)
        ? Vote.fromJSON(object.VoteB)
        : isSet(object.vote_b)
        ? Vote.fromJSON(object.vote_b)
        : undefined,
    };
  },

  toJSON(message: DuplicateVoteEvidence): unknown {
    const obj: any = {};
    if (message.pubKey !== undefined) {
      obj.PubKey = Any.toJSON(message.pubKey);
    }
    if (message.voteA !== undefined) {
      obj.VoteA = Vote.toJSON(message.voteA);
    }
    if (message.voteB !== undefined) {
      obj.VoteB = Vote.toJSON(message.voteB);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DuplicateVoteEvidence>, I>>(base?: I): DuplicateVoteEvidence {
    return DuplicateVoteEvidence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DuplicateVoteEvidence>, I>>(object: I): DuplicateVoteEvidence {
    const message = createBaseDuplicateVoteEvidence();
    message.pubKey = (object.pubKey !== undefined && object.pubKey !== null)
      ? Any.fromPartial(object.pubKey)
      : undefined;
    message.voteA = (object.voteA !== undefined && object.voteA !== null) ? Vote.fromPartial(object.voteA) : undefined;
    message.voteB = (object.voteB !== undefined && object.voteB !== null) ? Vote.fromPartial(object.voteB) : undefined;
    return message;
  },
};

function createBaseMockGoodEvidence(): MockGoodEvidence {
  return { height: 0n, address: "" };
}

export const MockGoodEvidence: MessageFns<MockGoodEvidence> = {
  encode(message: MockGoodEvidence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(8).sint64(message.height);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MockGoodEvidence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMockGoodEvidence();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.address = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MockGoodEvidence {
    return {
      height: isSet(object.Height) ? BigInt(object.Height) : isSet(object.height) ? BigInt(object.height) : 0n,
      address: isSet(object.Address)
        ? globalThis.String(object.Address)
        : isSet(object.address)
        ? globalThis.String(object.address)
        : "",
    };
  },

  toJSON(message: MockGoodEvidence): unknown {
    const obj: any = {};
    if (message.height !== undefined) {
      obj.Height = message.height.toString();
    }
    if (message.address !== undefined) {
      obj.Address = message.address;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MockGoodEvidence>, I>>(base?: I): MockGoodEvidence {
    return MockGoodEvidence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MockGoodEvidence>, I>>(object: I): MockGoodEvidence {
    const message = createBaseMockGoodEvidence();
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseMockRandomGoodEvidence(): MockRandomGoodEvidence {
  return { mockGoodEvidence: undefined };
}

export const MockRandomGoodEvidence: MessageFns<MockRandomGoodEvidence> = {
  encode(message: MockRandomGoodEvidence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mockGoodEvidence !== undefined) {
      MockGoodEvidence.encode(message.mockGoodEvidence, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MockRandomGoodEvidence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMockRandomGoodEvidence();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.mockGoodEvidence = MockGoodEvidence.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MockRandomGoodEvidence {
    return {
      mockGoodEvidence: isSet(object.MockGoodEvidence)
        ? MockGoodEvidence.fromJSON(object.MockGoodEvidence)
        : isSet(object.mock_good_evidence)
        ? MockGoodEvidence.fromJSON(object.mock_good_evidence)
        : undefined,
    };
  },

  toJSON(message: MockRandomGoodEvidence): unknown {
    const obj: any = {};
    if (message.mockGoodEvidence !== undefined) {
      obj.MockGoodEvidence = MockGoodEvidence.toJSON(message.mockGoodEvidence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MockRandomGoodEvidence>, I>>(base?: I): MockRandomGoodEvidence {
    return MockRandomGoodEvidence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MockRandomGoodEvidence>, I>>(object: I): MockRandomGoodEvidence {
    const message = createBaseMockRandomGoodEvidence();
    message.mockGoodEvidence = (object.mockGoodEvidence !== undefined && object.mockGoodEvidence !== null)
      ? MockGoodEvidence.fromPartial(object.mockGoodEvidence)
      : undefined;
    return message;
  },
};

function createBaseMockBadEvidence(): MockBadEvidence {
  return { mockGoodEvidence: undefined };
}

export const MockBadEvidence: MessageFns<MockBadEvidence> = {
  encode(message: MockBadEvidence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mockGoodEvidence !== undefined) {
      MockGoodEvidence.encode(message.mockGoodEvidence, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MockBadEvidence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMockBadEvidence();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.mockGoodEvidence = MockGoodEvidence.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MockBadEvidence {
    return {
      mockGoodEvidence: isSet(object.MockGoodEvidence)
        ? MockGoodEvidence.fromJSON(object.MockGoodEvidence)
        : isSet(object.mock_good_evidence)
        ? MockGoodEvidence.fromJSON(object.mock_good_evidence)
        : undefined,
    };
  },

  toJSON(message: MockBadEvidence): unknown {
    const obj: any = {};
    if (message.mockGoodEvidence !== undefined) {
      obj.MockGoodEvidence = MockGoodEvidence.toJSON(message.mockGoodEvidence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MockBadEvidence>, I>>(base?: I): MockBadEvidence {
    return MockBadEvidence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MockBadEvidence>, I>>(object: I): MockBadEvidence {
    const message = createBaseMockBadEvidence();
    message.mockGoodEvidence = (object.mockGoodEvidence !== undefined && object.mockGoodEvidence !== null)
      ? MockGoodEvidence.fromPartial(object.mockGoodEvidence)
      : undefined;
    return message;
  },
};

function createBaseTxResult(): TxResult {
  return { height: 0n, index: 0, tx: new Uint8Array(0), response: undefined };
}

export const TxResult: MessageFns<TxResult> = {
  encode(message: TxResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(8).sint64(message.height);
    }
    if (message.index !== 0) {
      writer.uint32(16).uint32(message.index);
    }
    if (message.tx.length !== 0) {
      writer.uint32(26).bytes(message.tx);
    }
    if (message.response !== undefined) {
      ResponseDeliverTx.encode(message.response, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TxResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTxResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.index = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.tx = reader.bytes();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.response = ResponseDeliverTx.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): TxResult {
    return {
      height: isSet(object.height) ? BigInt(object.height) : 0n,
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      tx: isSet(object.tx) ? bytesFromBase64(object.tx) : new Uint8Array(0),
      response: isSet(object.response) ? ResponseDeliverTx.fromJSON(object.response) : undefined,
    };
  },

  toJSON(message: TxResult): unknown {
    const obj: any = {};
    if (message.height !== undefined) {
      obj.height = message.height.toString();
    }
    if (message.index !== undefined) {
      obj.index = Math.round(message.index);
    }
    if (message.tx !== undefined) {
      obj.tx = base64FromBytes(message.tx);
    }
    if (message.response !== undefined) {
      obj.response = ResponseDeliverTx.toJSON(message.response);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TxResult>, I>>(base?: I): TxResult {
    return TxResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TxResult>, I>>(object: I): TxResult {
    const message = createBaseTxResult();
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.index = object.index ?? 0;
    message.tx = object.tx ?? new Uint8Array(0);
    message.response = (object.response !== undefined && object.response !== null)
      ? ResponseDeliverTx.fromPartial(object.response)
      : undefined;
    return message;
  },
};

function createBaseMockAppState(): MockAppState {
  return { accountOwner: "" };
}

export const MockAppState: MessageFns<MockAppState> = {
  encode(message: MockAppState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountOwner !== "") {
      writer.uint32(10).string(message.accountOwner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MockAppState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMockAppState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.accountOwner = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MockAppState {
    return {
      accountOwner: isSet(object.accountOwner)
        ? globalThis.String(object.accountOwner)
        : isSet(object.account_owner)
        ? globalThis.String(object.account_owner)
        : "",
    };
  },

  toJSON(message: MockAppState): unknown {
    const obj: any = {};
    if (message.accountOwner !== undefined) {
      obj.accountOwner = message.accountOwner;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MockAppState>, I>>(base?: I): MockAppState {
    return MockAppState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MockAppState>, I>>(object: I): MockAppState {
    const message = createBaseMockAppState();
    message.accountOwner = object.accountOwner ?? "";
    return message;
  },
};

function createBaseVoteSet(): VoteSet {
  return {};
}

export const VoteSet: MessageFns<VoteSet> = {
  encode(_: VoteSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoteSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVoteSet();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): VoteSet {
    return {};
  },

  toJSON(_: VoteSet): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<VoteSet>, I>>(base?: I): VoteSet {
    return VoteSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoteSet>, I>>(_: I): VoteSet {
    const message = createBaseVoteSet();
    return message;
  },
};

function createBaseTYPESBytesList(): TYPESBytesList {
  return { Value: [] };
}

export const TYPESBytesList: MessageFns<TYPESBytesList> = {
  encode(message: TYPESBytesList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.Value) {
      writer.uint32(10).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TYPESBytesList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTYPESBytesList();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.Value.push(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): TYPESBytesList {
    return { Value: globalThis.Array.isArray(object?.Value) ? object.Value.map((e: any) => bytesFromBase64(e)) : [] };
  },

  toJSON(message: TYPESBytesList): unknown {
    const obj: any = {};
    if (message.Value?.length) {
      obj.Value = message.Value.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TYPESBytesList>, I>>(base?: I): TYPESBytesList {
    return TYPESBytesList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TYPESBytesList>, I>>(object: I): TYPESBytesList {
    const message = createBaseTYPESBytesList();
    message.Value = object.Value?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Timestamp {
  if (o instanceof globalThis.Date) {
    return toTimestamp(o);
  } else if (typeof o === "string") {
    return toTimestamp(new globalThis.Date(o));
  } else {
    return Timestamp.fromJSON(o);
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
