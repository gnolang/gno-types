// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               v6.33.4
// source: gno/tm2/bft/abci.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../google/protobuf/any.js";
import { Timestamp } from "../../../google/protobuf/timestamp.js";
import { Proof } from "../crypto/merkle.js";

/** messages */
export interface RequestBase {
}

export interface RequestEcho {
  requestBase?: RequestBase | undefined;
  message: string;
}

export interface RequestFlush {
  requestBase?: RequestBase | undefined;
}

export interface RequestInfo {
  requestBase?: RequestBase | undefined;
}

export interface RequestSetOption {
  requestBase?: RequestBase | undefined;
  key: string;
  value: string;
}

export interface RequestInitChain {
  requestBase?: RequestBase | undefined;
  time?: Timestamp | undefined;
  chainId: string;
  consensusParams?: ConsensusParams | undefined;
  validators: ValidatorUpdate[];
  appState?: Any | undefined;
  initialHeight: bigint;
}

export interface RequestQuery {
  requestBase?: RequestBase | undefined;
  data: Uint8Array;
  path: string;
  height: bigint;
  prove: boolean;
}

export interface RequestBeginBlock {
  requestBase?: RequestBase | undefined;
  hash: Uint8Array;
  header?: Any | undefined;
  lastCommitInfo?: LastCommitInfo | undefined;
}

export interface RequestCheckTx {
  requestBase?: RequestBase | undefined;
  tx: Uint8Array;
  type: bigint;
}

export interface RequestDeliverTx {
  requestBase?: RequestBase | undefined;
  tx: Uint8Array;
}

export interface RequestEndBlock {
  requestBase?: RequestBase | undefined;
  height: bigint;
}

export interface RequestCommit {
  requestBase?: RequestBase | undefined;
}

export interface ResponseBase {
  error?: Any | undefined;
  data: Uint8Array;
  events: Any[];
  log: string;
  info: string;
}

export interface ResponseException {
  responseBase?: ResponseBase | undefined;
}

export interface ResponseEcho {
  responseBase?: ResponseBase | undefined;
  message: string;
}

export interface ResponseFlush {
  responseBase?: ResponseBase | undefined;
}

export interface ResponseInfo {
  responseBase?: ResponseBase | undefined;
  abciVersion: string;
  appVersion: string;
  lastBlockHeight: bigint;
  lastBlockAppHash: Uint8Array;
}

export interface ResponseSetOption {
  responseBase?: ResponseBase | undefined;
}

export interface ResponseInitChain {
  responseBase?: ResponseBase | undefined;
  consensusParams?: ConsensusParams | undefined;
  validators: ValidatorUpdate[];
  txResponses: ResponseDeliverTx[];
}

export interface ResponseQuery {
  responseBase?: ResponseBase | undefined;
  key: Uint8Array;
  value: Uint8Array;
  proof?: Proof | undefined;
  height: bigint;
}

export interface ResponseBeginBlock {
  responseBase?: ResponseBase | undefined;
}

export interface ResponseCheckTx {
  responseBase?: ResponseBase | undefined;
  gasWanted: bigint;
  gasUsed: bigint;
}

export interface ResponseDeliverTx {
  responseBase?: ResponseBase | undefined;
  gasWanted: bigint;
  gasUsed: bigint;
}

export interface ResponseEndBlock {
  responseBase?: ResponseBase | undefined;
  validatorUpdates: ValidatorUpdate[];
  consensusParams?: ConsensusParams | undefined;
  events: Any[];
}

export interface ResponseCommit {
  responseBase?: ResponseBase | undefined;
}

export interface StringError {
  value: string;
}

export interface ConsensusParams {
  block?: BlockParams | undefined;
  validator?: ValidatorParams | undefined;
}

export interface BlockParams {
  maxTxBytes: bigint;
  maxDataBytes: bigint;
  maxBlockBytes: bigint;
  maxGas: bigint;
  timeIotaMs: bigint;
}

export interface ValidatorParams {
  pubKeyTypeUrLs: string[];
}

export interface ValidatorUpdate {
  address: string;
  pubKey?: Any | undefined;
  power: bigint;
}

export interface LastCommitInfo {
  round: number;
  votes: VoteInfo[];
}

export interface VoteInfo {
  address: string;
  power: bigint;
  signedLastBlock: boolean;
}

export interface EventString {
  value: string;
}

export interface MockHeader {
  version: string;
  chainId: string;
  height: bigint;
  time?: Timestamp | undefined;
  numTxs: bigint;
  totalTxs: bigint;
}

function createBaseRequestBase(): RequestBase {
  return {};
}

export const RequestBase: MessageFns<RequestBase> = {
  encode(_: RequestBase, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestBase {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestBase();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): RequestBase {
    return {};
  },

  toJSON(_: RequestBase): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestBase>, I>>(base?: I): RequestBase {
    return RequestBase.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestBase>, I>>(_: I): RequestBase {
    const message = createBaseRequestBase();
    return message;
  },
};

function createBaseRequestEcho(): RequestEcho {
  return { requestBase: undefined, message: "" };
}

export const RequestEcho: MessageFns<RequestEcho> = {
  encode(message: RequestEcho, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEcho {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestEcho();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.message = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestEcho {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      message: isSet(object.Message)
        ? globalThis.String(object.Message)
        : isSet(object.message)
        ? globalThis.String(object.message)
        : "",
    };
  },

  toJSON(message: RequestEcho): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.message !== undefined) {
      obj.Message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEcho>, I>>(base?: I): RequestEcho {
    return RequestEcho.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEcho>, I>>(object: I): RequestEcho {
    const message = createBaseRequestEcho();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRequestFlush(): RequestFlush {
  return { requestBase: undefined };
}

export const RequestFlush: MessageFns<RequestFlush> = {
  encode(message: RequestFlush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestFlush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestFlush();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestFlush {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
    };
  },

  toJSON(message: RequestFlush): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestFlush>, I>>(base?: I): RequestFlush {
    return RequestFlush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestFlush>, I>>(object: I): RequestFlush {
    const message = createBaseRequestFlush();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    return message;
  },
};

function createBaseRequestInfo(): RequestInfo {
  return { requestBase: undefined };
}

export const RequestInfo: MessageFns<RequestInfo> = {
  encode(message: RequestInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestInfo {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
    };
  },

  toJSON(message: RequestInfo): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestInfo>, I>>(base?: I): RequestInfo {
    return RequestInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestInfo>, I>>(object: I): RequestInfo {
    const message = createBaseRequestInfo();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    return message;
  },
};

function createBaseRequestSetOption(): RequestSetOption {
  return { requestBase: undefined, key: "", value: "" };
}

export const RequestSetOption: MessageFns<RequestSetOption> = {
  encode(message: RequestSetOption, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(26).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestSetOption {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestSetOption();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.value = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestSetOption {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      key: isSet(object.Key) ? globalThis.String(object.Key) : isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.Value)
        ? globalThis.String(object.Value)
        : isSet(object.value)
        ? globalThis.String(object.value)
        : "",
    };
  },

  toJSON(message: RequestSetOption): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.key !== undefined) {
      obj.Key = message.key;
    }
    if (message.value !== undefined) {
      obj.Value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestSetOption>, I>>(base?: I): RequestSetOption {
    return RequestSetOption.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestSetOption>, I>>(object: I): RequestSetOption {
    const message = createBaseRequestSetOption();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseRequestInitChain(): RequestInitChain {
  return {
    requestBase: undefined,
    time: undefined,
    chainId: "",
    consensusParams: undefined,
    validators: [],
    appState: undefined,
    initialHeight: 0n,
  };
}

export const RequestInitChain: MessageFns<RequestInitChain> = {
  encode(message: RequestInitChain, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.time !== undefined) {
      Timestamp.encode(message.time, writer.uint32(18).fork()).join();
    }
    if (message.chainId !== "") {
      writer.uint32(26).string(message.chainId);
    }
    if (message.consensusParams !== undefined) {
      ConsensusParams.encode(message.consensusParams, writer.uint32(34).fork()).join();
    }
    for (const v of message.validators) {
      ValidatorUpdate.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.appState !== undefined) {
      Any.encode(message.appState, writer.uint32(50).fork()).join();
    }
    if (message.initialHeight !== 0n) {
      if (BigInt.asIntN(64, message.initialHeight) !== message.initialHeight) {
        throw new globalThis.Error("value provided for field message.initialHeight of type sint64 too large");
      }
      writer.uint32(56).sint64(message.initialHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestInitChain {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestInitChain();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.time = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.chainId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.consensusParams = ConsensusParams.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.validators.push(ValidatorUpdate.decode(reader, reader.uint32()));
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.appState = Any.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.initialHeight = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestInitChain {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      time: isSet(object.Time)
        ? fromJsonTimestamp(object.Time)
        : isSet(object.time)
        ? fromJsonTimestamp(object.time)
        : undefined,
      chainId: isSet(object.ChainID)
        ? globalThis.String(object.ChainID)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      consensusParams: isSet(object.ConsensusParams)
        ? ConsensusParams.fromJSON(object.ConsensusParams)
        : isSet(object.consensus_params)
        ? ConsensusParams.fromJSON(object.consensus_params)
        : undefined,
      validators: globalThis.Array.isArray(object?.Validators)
        ? object.Validators.map((e: any) => ValidatorUpdate.fromJSON(e))
        : globalThis.Array.isArray(object?.validators)
        ? object.validators.map((e: any) => ValidatorUpdate.fromJSON(e))
        : [],
      appState: isSet(object.AppState)
        ? Any.fromJSON(object.AppState)
        : isSet(object.app_state)
        ? Any.fromJSON(object.app_state)
        : undefined,
      initialHeight: isSet(object.InitialHeight)
        ? BigInt(object.InitialHeight)
        : isSet(object.initial_height)
        ? BigInt(object.initial_height)
        : 0n,
    };
  },

  toJSON(message: RequestInitChain): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.time !== undefined) {
      obj.Time = fromTimestamp(message.time).toISOString();
    }
    if (message.chainId !== undefined) {
      obj.ChainID = message.chainId;
    }
    if (message.consensusParams !== undefined) {
      obj.ConsensusParams = ConsensusParams.toJSON(message.consensusParams);
    }
    if (message.validators?.length) {
      obj.Validators = message.validators.map((e) => ValidatorUpdate.toJSON(e));
    }
    if (message.appState !== undefined) {
      obj.AppState = Any.toJSON(message.appState);
    }
    if (message.initialHeight !== undefined) {
      obj.InitialHeight = message.initialHeight.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestInitChain>, I>>(base?: I): RequestInitChain {
    return RequestInitChain.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestInitChain>, I>>(object: I): RequestInitChain {
    const message = createBaseRequestInitChain();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.time = (object.time !== undefined && object.time !== null) ? Timestamp.fromPartial(object.time) : undefined;
    message.chainId = object.chainId ?? "";
    message.consensusParams = (object.consensusParams !== undefined && object.consensusParams !== null)
      ? ConsensusParams.fromPartial(object.consensusParams)
      : undefined;
    message.validators = object.validators?.map((e) => ValidatorUpdate.fromPartial(e)) || [];
    message.appState = (object.appState !== undefined && object.appState !== null)
      ? Any.fromPartial(object.appState)
      : undefined;
    message.initialHeight = (object.initialHeight !== undefined && object.initialHeight !== null)
      ? BigInt(object.initialHeight)
      : 0n;
    return message;
  },
};

function createBaseRequestQuery(): RequestQuery {
  return { requestBase: undefined, data: new Uint8Array(0), path: "", height: 0n, prove: false };
}

export const RequestQuery: MessageFns<RequestQuery> = {
  encode(message: RequestQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.path !== "") {
      writer.uint32(26).string(message.path);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(32).sint64(message.height);
    }
    if (message.prove !== false) {
      writer.uint32(40).bool(message.prove);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestQuery();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.data = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.path = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.prove = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestQuery {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      data: isSet(object.Data)
        ? bytesFromBase64(object.Data)
        : isSet(object.data)
        ? bytesFromBase64(object.data)
        : new Uint8Array(0),
      path: isSet(object.Path)
        ? globalThis.String(object.Path)
        : isSet(object.path)
        ? globalThis.String(object.path)
        : "",
      height: isSet(object.Height) ? BigInt(object.Height) : isSet(object.height) ? BigInt(object.height) : 0n,
      prove: isSet(object.Prove)
        ? globalThis.Boolean(object.Prove)
        : isSet(object.prove)
        ? globalThis.Boolean(object.prove)
        : false,
    };
  },

  toJSON(message: RequestQuery): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.data !== undefined) {
      obj.Data = base64FromBytes(message.data);
    }
    if (message.path !== undefined) {
      obj.Path = message.path;
    }
    if (message.height !== undefined) {
      obj.Height = message.height.toString();
    }
    if (message.prove !== undefined) {
      obj.Prove = message.prove;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestQuery>, I>>(base?: I): RequestQuery {
    return RequestQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestQuery>, I>>(object: I): RequestQuery {
    const message = createBaseRequestQuery();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.data = object.data ?? new Uint8Array(0);
    message.path = object.path ?? "";
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.prove = object.prove ?? false;
    return message;
  },
};

function createBaseRequestBeginBlock(): RequestBeginBlock {
  return { requestBase: undefined, hash: new Uint8Array(0), header: undefined, lastCommitInfo: undefined };
}

export const RequestBeginBlock: MessageFns<RequestBeginBlock> = {
  encode(message: RequestBeginBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.hash.length !== 0) {
      writer.uint32(18).bytes(message.hash);
    }
    if (message.header !== undefined) {
      Any.encode(message.header, writer.uint32(26).fork()).join();
    }
    if (message.lastCommitInfo !== undefined) {
      LastCommitInfo.encode(message.lastCommitInfo, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestBeginBlock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestBeginBlock();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.hash = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.header = Any.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.lastCommitInfo = LastCommitInfo.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestBeginBlock {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      hash: isSet(object.Hash)
        ? bytesFromBase64(object.Hash)
        : isSet(object.hash)
        ? bytesFromBase64(object.hash)
        : new Uint8Array(0),
      header: isSet(object.Header)
        ? Any.fromJSON(object.Header)
        : isSet(object.header)
        ? Any.fromJSON(object.header)
        : undefined,
      lastCommitInfo: isSet(object.LastCommitInfo)
        ? LastCommitInfo.fromJSON(object.LastCommitInfo)
        : isSet(object.last_commit_info)
        ? LastCommitInfo.fromJSON(object.last_commit_info)
        : undefined,
    };
  },

  toJSON(message: RequestBeginBlock): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.hash !== undefined) {
      obj.Hash = base64FromBytes(message.hash);
    }
    if (message.header !== undefined) {
      obj.Header = Any.toJSON(message.header);
    }
    if (message.lastCommitInfo !== undefined) {
      obj.LastCommitInfo = LastCommitInfo.toJSON(message.lastCommitInfo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestBeginBlock>, I>>(base?: I): RequestBeginBlock {
    return RequestBeginBlock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestBeginBlock>, I>>(object: I): RequestBeginBlock {
    const message = createBaseRequestBeginBlock();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.hash = object.hash ?? new Uint8Array(0);
    message.header = (object.header !== undefined && object.header !== null)
      ? Any.fromPartial(object.header)
      : undefined;
    message.lastCommitInfo = (object.lastCommitInfo !== undefined && object.lastCommitInfo !== null)
      ? LastCommitInfo.fromPartial(object.lastCommitInfo)
      : undefined;
    return message;
  },
};

function createBaseRequestCheckTx(): RequestCheckTx {
  return { requestBase: undefined, tx: new Uint8Array(0), type: 0n };
}

export const RequestCheckTx: MessageFns<RequestCheckTx> = {
  encode(message: RequestCheckTx, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.tx.length !== 0) {
      writer.uint32(18).bytes(message.tx);
    }
    if (message.type !== 0n) {
      if (BigInt.asIntN(64, message.type) !== message.type) {
        throw new globalThis.Error("value provided for field message.type of type sint64 too large");
      }
      writer.uint32(24).sint64(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestCheckTx {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestCheckTx();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.tx = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.type = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestCheckTx {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      tx: isSet(object.Tx)
        ? bytesFromBase64(object.Tx)
        : isSet(object.tx)
        ? bytesFromBase64(object.tx)
        : new Uint8Array(0),
      type: isSet(object.Type) ? BigInt(object.Type) : isSet(object.type) ? BigInt(object.type) : 0n,
    };
  },

  toJSON(message: RequestCheckTx): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.tx !== undefined) {
      obj.Tx = base64FromBytes(message.tx);
    }
    if (message.type !== undefined) {
      obj.Type = message.type.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestCheckTx>, I>>(base?: I): RequestCheckTx {
    return RequestCheckTx.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestCheckTx>, I>>(object: I): RequestCheckTx {
    const message = createBaseRequestCheckTx();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.tx = object.tx ?? new Uint8Array(0);
    message.type = (object.type !== undefined && object.type !== null) ? BigInt(object.type) : 0n;
    return message;
  },
};

function createBaseRequestDeliverTx(): RequestDeliverTx {
  return { requestBase: undefined, tx: new Uint8Array(0) };
}

export const RequestDeliverTx: MessageFns<RequestDeliverTx> = {
  encode(message: RequestDeliverTx, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.tx.length !== 0) {
      writer.uint32(18).bytes(message.tx);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestDeliverTx {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestDeliverTx();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.tx = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestDeliverTx {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      tx: isSet(object.Tx)
        ? bytesFromBase64(object.Tx)
        : isSet(object.tx)
        ? bytesFromBase64(object.tx)
        : new Uint8Array(0),
    };
  },

  toJSON(message: RequestDeliverTx): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.tx !== undefined) {
      obj.Tx = base64FromBytes(message.tx);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestDeliverTx>, I>>(base?: I): RequestDeliverTx {
    return RequestDeliverTx.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestDeliverTx>, I>>(object: I): RequestDeliverTx {
    const message = createBaseRequestDeliverTx();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.tx = object.tx ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRequestEndBlock(): RequestEndBlock {
  return { requestBase: undefined, height: 0n };
}

export const RequestEndBlock: MessageFns<RequestEndBlock> = {
  encode(message: RequestEndBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(16).sint64(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEndBlock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestEndBlock();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestEndBlock {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
      height: isSet(object.Height) ? BigInt(object.Height) : isSet(object.height) ? BigInt(object.height) : 0n,
    };
  },

  toJSON(message: RequestEndBlock): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    if (message.height !== undefined) {
      obj.Height = message.height.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEndBlock>, I>>(base?: I): RequestEndBlock {
    return RequestEndBlock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEndBlock>, I>>(object: I): RequestEndBlock {
    const message = createBaseRequestEndBlock();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    return message;
  },
};

function createBaseRequestCommit(): RequestCommit {
  return { requestBase: undefined };
}

export const RequestCommit: MessageFns<RequestCommit> = {
  encode(message: RequestCommit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestBase !== undefined) {
      RequestBase.encode(message.requestBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestCommit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestCommit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requestBase = RequestBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestCommit {
    return {
      requestBase: isSet(object.RequestBase)
        ? RequestBase.fromJSON(object.RequestBase)
        : isSet(object.request_base)
        ? RequestBase.fromJSON(object.request_base)
        : undefined,
    };
  },

  toJSON(message: RequestCommit): unknown {
    const obj: any = {};
    if (message.requestBase !== undefined) {
      obj.RequestBase = RequestBase.toJSON(message.requestBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestCommit>, I>>(base?: I): RequestCommit {
    return RequestCommit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestCommit>, I>>(object: I): RequestCommit {
    const message = createBaseRequestCommit();
    message.requestBase = (object.requestBase !== undefined && object.requestBase !== null)
      ? RequestBase.fromPartial(object.requestBase)
      : undefined;
    return message;
  },
};

function createBaseResponseBase(): ResponseBase {
  return { error: undefined, data: new Uint8Array(0), events: [], log: "", info: "" };
}

export const ResponseBase: MessageFns<ResponseBase> = {
  encode(message: ResponseBase, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      Any.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    for (const v of message.events) {
      Any.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.log !== "") {
      writer.uint32(34).string(message.log);
    }
    if (message.info !== "") {
      writer.uint32(42).string(message.info);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseBase {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseBase();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.error = Any.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.data = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.events.push(Any.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.log = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.info = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseBase {
    return {
      error: isSet(object.Error)
        ? Any.fromJSON(object.Error)
        : isSet(object.error)
        ? Any.fromJSON(object.error)
        : undefined,
      data: isSet(object.Data)
        ? bytesFromBase64(object.Data)
        : isSet(object.data)
        ? bytesFromBase64(object.data)
        : new Uint8Array(0),
      events: globalThis.Array.isArray(object?.Events)
        ? object.Events.map((e: any) => Any.fromJSON(e))
        : globalThis.Array.isArray(object?.events)
        ? object.events.map((e: any) => Any.fromJSON(e))
        : [],
      log: isSet(object.Log) ? globalThis.String(object.Log) : isSet(object.log) ? globalThis.String(object.log) : "",
      info: isSet(object.Info)
        ? globalThis.String(object.Info)
        : isSet(object.info)
        ? globalThis.String(object.info)
        : "",
    };
  },

  toJSON(message: ResponseBase): unknown {
    const obj: any = {};
    if (message.error !== undefined) {
      obj.Error = Any.toJSON(message.error);
    }
    if (message.data !== undefined) {
      obj.Data = base64FromBytes(message.data);
    }
    if (message.events?.length) {
      obj.Events = message.events.map((e) => Any.toJSON(e));
    }
    if (message.log !== undefined) {
      obj.Log = message.log;
    }
    if (message.info !== undefined) {
      obj.Info = message.info;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseBase>, I>>(base?: I): ResponseBase {
    return ResponseBase.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseBase>, I>>(object: I): ResponseBase {
    const message = createBaseResponseBase();
    message.error = (object.error !== undefined && object.error !== null) ? Any.fromPartial(object.error) : undefined;
    message.data = object.data ?? new Uint8Array(0);
    message.events = object.events?.map((e) => Any.fromPartial(e)) || [];
    message.log = object.log ?? "";
    message.info = object.info ?? "";
    return message;
  },
};

function createBaseResponseException(): ResponseException {
  return { responseBase: undefined };
}

export const ResponseException: MessageFns<ResponseException> = {
  encode(message: ResponseException, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseException {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseException();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseException {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
    };
  },

  toJSON(message: ResponseException): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseException>, I>>(base?: I): ResponseException {
    return ResponseException.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseException>, I>>(object: I): ResponseException {
    const message = createBaseResponseException();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    return message;
  },
};

function createBaseResponseEcho(): ResponseEcho {
  return { responseBase: undefined, message: "" };
}

export const ResponseEcho: MessageFns<ResponseEcho> = {
  encode(message: ResponseEcho, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseEcho {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseEcho();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.message = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseEcho {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
      message: isSet(object.Message)
        ? globalThis.String(object.Message)
        : isSet(object.message)
        ? globalThis.String(object.message)
        : "",
    };
  },

  toJSON(message: ResponseEcho): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    if (message.message !== undefined) {
      obj.Message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseEcho>, I>>(base?: I): ResponseEcho {
    return ResponseEcho.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseEcho>, I>>(object: I): ResponseEcho {
    const message = createBaseResponseEcho();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseResponseFlush(): ResponseFlush {
  return { responseBase: undefined };
}

export const ResponseFlush: MessageFns<ResponseFlush> = {
  encode(message: ResponseFlush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseFlush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseFlush();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseFlush {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
    };
  },

  toJSON(message: ResponseFlush): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseFlush>, I>>(base?: I): ResponseFlush {
    return ResponseFlush.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseFlush>, I>>(object: I): ResponseFlush {
    const message = createBaseResponseFlush();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    return message;
  },
};

function createBaseResponseInfo(): ResponseInfo {
  return {
    responseBase: undefined,
    abciVersion: "",
    appVersion: "",
    lastBlockHeight: 0n,
    lastBlockAppHash: new Uint8Array(0),
  };
}

export const ResponseInfo: MessageFns<ResponseInfo> = {
  encode(message: ResponseInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    if (message.abciVersion !== "") {
      writer.uint32(18).string(message.abciVersion);
    }
    if (message.appVersion !== "") {
      writer.uint32(26).string(message.appVersion);
    }
    if (message.lastBlockHeight !== 0n) {
      if (BigInt.asIntN(64, message.lastBlockHeight) !== message.lastBlockHeight) {
        throw new globalThis.Error("value provided for field message.lastBlockHeight of type sint64 too large");
      }
      writer.uint32(32).sint64(message.lastBlockHeight);
    }
    if (message.lastBlockAppHash.length !== 0) {
      writer.uint32(42).bytes(message.lastBlockAppHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.abciVersion = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.appVersion = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.lastBlockHeight = reader.sint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.lastBlockAppHash = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseInfo {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
      abciVersion: isSet(object.ABCIVersion)
        ? globalThis.String(object.ABCIVersion)
        : isSet(object.abci_version)
        ? globalThis.String(object.abci_version)
        : "",
      appVersion: isSet(object.AppVersion)
        ? globalThis.String(object.AppVersion)
        : isSet(object.app_version)
        ? globalThis.String(object.app_version)
        : "",
      lastBlockHeight: isSet(object.LastBlockHeight)
        ? BigInt(object.LastBlockHeight)
        : isSet(object.last_block_height)
        ? BigInt(object.last_block_height)
        : 0n,
      lastBlockAppHash: isSet(object.LastBlockAppHash)
        ? bytesFromBase64(object.LastBlockAppHash)
        : isSet(object.last_block_app_hash)
        ? bytesFromBase64(object.last_block_app_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: ResponseInfo): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    if (message.abciVersion !== undefined) {
      obj.ABCIVersion = message.abciVersion;
    }
    if (message.appVersion !== undefined) {
      obj.AppVersion = message.appVersion;
    }
    if (message.lastBlockHeight !== undefined) {
      obj.LastBlockHeight = message.lastBlockHeight.toString();
    }
    if (message.lastBlockAppHash !== undefined) {
      obj.LastBlockAppHash = base64FromBytes(message.lastBlockAppHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseInfo>, I>>(base?: I): ResponseInfo {
    return ResponseInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseInfo>, I>>(object: I): ResponseInfo {
    const message = createBaseResponseInfo();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    message.abciVersion = object.abciVersion ?? "";
    message.appVersion = object.appVersion ?? "";
    message.lastBlockHeight = (object.lastBlockHeight !== undefined && object.lastBlockHeight !== null)
      ? BigInt(object.lastBlockHeight)
      : 0n;
    message.lastBlockAppHash = object.lastBlockAppHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseResponseSetOption(): ResponseSetOption {
  return { responseBase: undefined };
}

export const ResponseSetOption: MessageFns<ResponseSetOption> = {
  encode(message: ResponseSetOption, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseSetOption {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseSetOption();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseSetOption {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
    };
  },

  toJSON(message: ResponseSetOption): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseSetOption>, I>>(base?: I): ResponseSetOption {
    return ResponseSetOption.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseSetOption>, I>>(object: I): ResponseSetOption {
    const message = createBaseResponseSetOption();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    return message;
  },
};

function createBaseResponseInitChain(): ResponseInitChain {
  return { responseBase: undefined, consensusParams: undefined, validators: [], txResponses: [] };
}

export const ResponseInitChain: MessageFns<ResponseInitChain> = {
  encode(message: ResponseInitChain, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    if (message.consensusParams !== undefined) {
      ConsensusParams.encode(message.consensusParams, writer.uint32(18).fork()).join();
    }
    for (const v of message.validators) {
      ValidatorUpdate.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.txResponses) {
      ResponseDeliverTx.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseInitChain {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseInitChain();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.consensusParams = ConsensusParams.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.validators.push(ValidatorUpdate.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.txResponses.push(ResponseDeliverTx.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseInitChain {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
      consensusParams: isSet(object.ConsensusParams)
        ? ConsensusParams.fromJSON(object.ConsensusParams)
        : isSet(object.consensus_params)
        ? ConsensusParams.fromJSON(object.consensus_params)
        : undefined,
      validators: globalThis.Array.isArray(object?.Validators)
        ? object.Validators.map((e: any) => ValidatorUpdate.fromJSON(e))
        : globalThis.Array.isArray(object?.validators)
        ? object.validators.map((e: any) => ValidatorUpdate.fromJSON(e))
        : [],
      txResponses: globalThis.Array.isArray(object?.TxResponses)
        ? object.TxResponses.map((e: any) => ResponseDeliverTx.fromJSON(e))
        : globalThis.Array.isArray(object?.tx_responses)
        ? object.tx_responses.map((e: any) => ResponseDeliverTx.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ResponseInitChain): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    if (message.consensusParams !== undefined) {
      obj.ConsensusParams = ConsensusParams.toJSON(message.consensusParams);
    }
    if (message.validators?.length) {
      obj.Validators = message.validators.map((e) => ValidatorUpdate.toJSON(e));
    }
    if (message.txResponses?.length) {
      obj.TxResponses = message.txResponses.map((e) => ResponseDeliverTx.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseInitChain>, I>>(base?: I): ResponseInitChain {
    return ResponseInitChain.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseInitChain>, I>>(object: I): ResponseInitChain {
    const message = createBaseResponseInitChain();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    message.consensusParams = (object.consensusParams !== undefined && object.consensusParams !== null)
      ? ConsensusParams.fromPartial(object.consensusParams)
      : undefined;
    message.validators = object.validators?.map((e) => ValidatorUpdate.fromPartial(e)) || [];
    message.txResponses = object.txResponses?.map((e) => ResponseDeliverTx.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResponseQuery(): ResponseQuery {
  return { responseBase: undefined, key: new Uint8Array(0), value: new Uint8Array(0), proof: undefined, height: 0n };
}

export const ResponseQuery: MessageFns<ResponseQuery> = {
  encode(message: ResponseQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    if (message.key.length !== 0) {
      writer.uint32(18).bytes(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(26).bytes(message.value);
    }
    if (message.proof !== undefined) {
      Proof.encode(message.proof, writer.uint32(34).fork()).join();
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(40).sint64(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseQuery();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.key = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.value = reader.bytes();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.proof = Proof.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseQuery {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
      key: isSet(object.Key)
        ? bytesFromBase64(object.Key)
        : isSet(object.key)
        ? bytesFromBase64(object.key)
        : new Uint8Array(0),
      value: isSet(object.Value)
        ? bytesFromBase64(object.Value)
        : isSet(object.value)
        ? bytesFromBase64(object.value)
        : new Uint8Array(0),
      proof: isSet(object.Proof)
        ? Proof.fromJSON(object.Proof)
        : isSet(object.proof)
        ? Proof.fromJSON(object.proof)
        : undefined,
      height: isSet(object.Height) ? BigInt(object.Height) : isSet(object.height) ? BigInt(object.height) : 0n,
    };
  },

  toJSON(message: ResponseQuery): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    if (message.key !== undefined) {
      obj.Key = base64FromBytes(message.key);
    }
    if (message.value !== undefined) {
      obj.Value = base64FromBytes(message.value);
    }
    if (message.proof !== undefined) {
      obj.Proof = Proof.toJSON(message.proof);
    }
    if (message.height !== undefined) {
      obj.Height = message.height.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseQuery>, I>>(base?: I): ResponseQuery {
    return ResponseQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseQuery>, I>>(object: I): ResponseQuery {
    const message = createBaseResponseQuery();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    message.key = object.key ?? new Uint8Array(0);
    message.value = object.value ?? new Uint8Array(0);
    message.proof = (object.proof !== undefined && object.proof !== null) ? Proof.fromPartial(object.proof) : undefined;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    return message;
  },
};

function createBaseResponseBeginBlock(): ResponseBeginBlock {
  return { responseBase: undefined };
}

export const ResponseBeginBlock: MessageFns<ResponseBeginBlock> = {
  encode(message: ResponseBeginBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseBeginBlock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseBeginBlock();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseBeginBlock {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
    };
  },

  toJSON(message: ResponseBeginBlock): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseBeginBlock>, I>>(base?: I): ResponseBeginBlock {
    return ResponseBeginBlock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseBeginBlock>, I>>(object: I): ResponseBeginBlock {
    const message = createBaseResponseBeginBlock();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    return message;
  },
};

function createBaseResponseCheckTx(): ResponseCheckTx {
  return { responseBase: undefined, gasWanted: 0n, gasUsed: 0n };
}

export const ResponseCheckTx: MessageFns<ResponseCheckTx> = {
  encode(message: ResponseCheckTx, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    if (message.gasWanted !== 0n) {
      if (BigInt.asIntN(64, message.gasWanted) !== message.gasWanted) {
        throw new globalThis.Error("value provided for field message.gasWanted of type sint64 too large");
      }
      writer.uint32(16).sint64(message.gasWanted);
    }
    if (message.gasUsed !== 0n) {
      if (BigInt.asIntN(64, message.gasUsed) !== message.gasUsed) {
        throw new globalThis.Error("value provided for field message.gasUsed of type sint64 too large");
      }
      writer.uint32(24).sint64(message.gasUsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseCheckTx {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseCheckTx();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.gasWanted = reader.sint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.gasUsed = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseCheckTx {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
      gasWanted: isSet(object.GasWanted)
        ? BigInt(object.GasWanted)
        : isSet(object.gas_wanted)
        ? BigInt(object.gas_wanted)
        : 0n,
      gasUsed: isSet(object.GasUsed) ? BigInt(object.GasUsed) : isSet(object.gas_used) ? BigInt(object.gas_used) : 0n,
    };
  },

  toJSON(message: ResponseCheckTx): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    if (message.gasWanted !== undefined) {
      obj.GasWanted = message.gasWanted.toString();
    }
    if (message.gasUsed !== undefined) {
      obj.GasUsed = message.gasUsed.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseCheckTx>, I>>(base?: I): ResponseCheckTx {
    return ResponseCheckTx.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseCheckTx>, I>>(object: I): ResponseCheckTx {
    const message = createBaseResponseCheckTx();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    message.gasWanted = (object.gasWanted !== undefined && object.gasWanted !== null) ? BigInt(object.gasWanted) : 0n;
    message.gasUsed = (object.gasUsed !== undefined && object.gasUsed !== null) ? BigInt(object.gasUsed) : 0n;
    return message;
  },
};

function createBaseResponseDeliverTx(): ResponseDeliverTx {
  return { responseBase: undefined, gasWanted: 0n, gasUsed: 0n };
}

export const ResponseDeliverTx: MessageFns<ResponseDeliverTx> = {
  encode(message: ResponseDeliverTx, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    if (message.gasWanted !== 0n) {
      if (BigInt.asIntN(64, message.gasWanted) !== message.gasWanted) {
        throw new globalThis.Error("value provided for field message.gasWanted of type sint64 too large");
      }
      writer.uint32(16).sint64(message.gasWanted);
    }
    if (message.gasUsed !== 0n) {
      if (BigInt.asIntN(64, message.gasUsed) !== message.gasUsed) {
        throw new globalThis.Error("value provided for field message.gasUsed of type sint64 too large");
      }
      writer.uint32(24).sint64(message.gasUsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseDeliverTx {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseDeliverTx();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.gasWanted = reader.sint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.gasUsed = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseDeliverTx {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
      gasWanted: isSet(object.GasWanted)
        ? BigInt(object.GasWanted)
        : isSet(object.gas_wanted)
        ? BigInt(object.gas_wanted)
        : 0n,
      gasUsed: isSet(object.GasUsed) ? BigInt(object.GasUsed) : isSet(object.gas_used) ? BigInt(object.gas_used) : 0n,
    };
  },

  toJSON(message: ResponseDeliverTx): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    if (message.gasWanted !== undefined) {
      obj.GasWanted = message.gasWanted.toString();
    }
    if (message.gasUsed !== undefined) {
      obj.GasUsed = message.gasUsed.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseDeliverTx>, I>>(base?: I): ResponseDeliverTx {
    return ResponseDeliverTx.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseDeliverTx>, I>>(object: I): ResponseDeliverTx {
    const message = createBaseResponseDeliverTx();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    message.gasWanted = (object.gasWanted !== undefined && object.gasWanted !== null) ? BigInt(object.gasWanted) : 0n;
    message.gasUsed = (object.gasUsed !== undefined && object.gasUsed !== null) ? BigInt(object.gasUsed) : 0n;
    return message;
  },
};

function createBaseResponseEndBlock(): ResponseEndBlock {
  return { responseBase: undefined, validatorUpdates: [], consensusParams: undefined, events: [] };
}

export const ResponseEndBlock: MessageFns<ResponseEndBlock> = {
  encode(message: ResponseEndBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    for (const v of message.validatorUpdates) {
      ValidatorUpdate.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.consensusParams !== undefined) {
      ConsensusParams.encode(message.consensusParams, writer.uint32(26).fork()).join();
    }
    for (const v of message.events) {
      Any.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseEndBlock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseEndBlock();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.validatorUpdates.push(ValidatorUpdate.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.consensusParams = ConsensusParams.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.events.push(Any.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseEndBlock {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
      validatorUpdates: globalThis.Array.isArray(object?.ValidatorUpdates)
        ? object.ValidatorUpdates.map((e: any) => ValidatorUpdate.fromJSON(e))
        : globalThis.Array.isArray(object?.validator_updates)
        ? object.validator_updates.map((e: any) => ValidatorUpdate.fromJSON(e))
        : [],
      consensusParams: isSet(object.ConsensusParams)
        ? ConsensusParams.fromJSON(object.ConsensusParams)
        : isSet(object.consensus_params)
        ? ConsensusParams.fromJSON(object.consensus_params)
        : undefined,
      events: globalThis.Array.isArray(object?.Events)
        ? object.Events.map((e: any) => Any.fromJSON(e))
        : globalThis.Array.isArray(object?.events)
        ? object.events.map((e: any) => Any.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ResponseEndBlock): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    if (message.validatorUpdates?.length) {
      obj.ValidatorUpdates = message.validatorUpdates.map((e) => ValidatorUpdate.toJSON(e));
    }
    if (message.consensusParams !== undefined) {
      obj.ConsensusParams = ConsensusParams.toJSON(message.consensusParams);
    }
    if (message.events?.length) {
      obj.Events = message.events.map((e) => Any.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseEndBlock>, I>>(base?: I): ResponseEndBlock {
    return ResponseEndBlock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseEndBlock>, I>>(object: I): ResponseEndBlock {
    const message = createBaseResponseEndBlock();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    message.validatorUpdates = object.validatorUpdates?.map((e) => ValidatorUpdate.fromPartial(e)) || [];
    message.consensusParams = (object.consensusParams !== undefined && object.consensusParams !== null)
      ? ConsensusParams.fromPartial(object.consensusParams)
      : undefined;
    message.events = object.events?.map((e) => Any.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResponseCommit(): ResponseCommit {
  return { responseBase: undefined };
}

export const ResponseCommit: MessageFns<ResponseCommit> = {
  encode(message: ResponseCommit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseBase !== undefined) {
      ResponseBase.encode(message.responseBase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseCommit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponseCommit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.responseBase = ResponseBase.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ResponseCommit {
    return {
      responseBase: isSet(object.ResponseBase)
        ? ResponseBase.fromJSON(object.ResponseBase)
        : isSet(object.response_base)
        ? ResponseBase.fromJSON(object.response_base)
        : undefined,
    };
  },

  toJSON(message: ResponseCommit): unknown {
    const obj: any = {};
    if (message.responseBase !== undefined) {
      obj.ResponseBase = ResponseBase.toJSON(message.responseBase);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResponseCommit>, I>>(base?: I): ResponseCommit {
    return ResponseCommit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseCommit>, I>>(object: I): ResponseCommit {
    const message = createBaseResponseCommit();
    message.responseBase = (object.responseBase !== undefined && object.responseBase !== null)
      ? ResponseBase.fromPartial(object.responseBase)
      : undefined;
    return message;
  },
};

function createBaseStringError(): StringError {
  return { value: "" };
}

export const StringError: MessageFns<StringError> = {
  encode(message: StringError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StringError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStringError();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.value = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StringError {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: StringError): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StringError>, I>>(base?: I): StringError {
    return StringError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StringError>, I>>(object: I): StringError {
    const message = createBaseStringError();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseConsensusParams(): ConsensusParams {
  return { block: undefined, validator: undefined };
}

export const ConsensusParams: MessageFns<ConsensusParams> = {
  encode(message: ConsensusParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.block !== undefined) {
      BlockParams.encode(message.block, writer.uint32(10).fork()).join();
    }
    if (message.validator !== undefined) {
      ValidatorParams.encode(message.validator, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConsensusParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConsensusParams();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.block = BlockParams.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.validator = ValidatorParams.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConsensusParams {
    return {
      block: isSet(object.Block)
        ? BlockParams.fromJSON(object.Block)
        : isSet(object.block)
        ? BlockParams.fromJSON(object.block)
        : undefined,
      validator: isSet(object.Validator)
        ? ValidatorParams.fromJSON(object.Validator)
        : isSet(object.validator)
        ? ValidatorParams.fromJSON(object.validator)
        : undefined,
    };
  },

  toJSON(message: ConsensusParams): unknown {
    const obj: any = {};
    if (message.block !== undefined) {
      obj.Block = BlockParams.toJSON(message.block);
    }
    if (message.validator !== undefined) {
      obj.Validator = ValidatorParams.toJSON(message.validator);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConsensusParams>, I>>(base?: I): ConsensusParams {
    return ConsensusParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConsensusParams>, I>>(object: I): ConsensusParams {
    const message = createBaseConsensusParams();
    message.block = (object.block !== undefined && object.block !== null)
      ? BlockParams.fromPartial(object.block)
      : undefined;
    message.validator = (object.validator !== undefined && object.validator !== null)
      ? ValidatorParams.fromPartial(object.validator)
      : undefined;
    return message;
  },
};

function createBaseBlockParams(): BlockParams {
  return { maxTxBytes: 0n, maxDataBytes: 0n, maxBlockBytes: 0n, maxGas: 0n, timeIotaMs: 0n };
}

export const BlockParams: MessageFns<BlockParams> = {
  encode(message: BlockParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxTxBytes !== 0n) {
      if (BigInt.asIntN(64, message.maxTxBytes) !== message.maxTxBytes) {
        throw new globalThis.Error("value provided for field message.maxTxBytes of type sint64 too large");
      }
      writer.uint32(8).sint64(message.maxTxBytes);
    }
    if (message.maxDataBytes !== 0n) {
      if (BigInt.asIntN(64, message.maxDataBytes) !== message.maxDataBytes) {
        throw new globalThis.Error("value provided for field message.maxDataBytes of type sint64 too large");
      }
      writer.uint32(16).sint64(message.maxDataBytes);
    }
    if (message.maxBlockBytes !== 0n) {
      if (BigInt.asIntN(64, message.maxBlockBytes) !== message.maxBlockBytes) {
        throw new globalThis.Error("value provided for field message.maxBlockBytes of type sint64 too large");
      }
      writer.uint32(24).sint64(message.maxBlockBytes);
    }
    if (message.maxGas !== 0n) {
      if (BigInt.asIntN(64, message.maxGas) !== message.maxGas) {
        throw new globalThis.Error("value provided for field message.maxGas of type sint64 too large");
      }
      writer.uint32(32).sint64(message.maxGas);
    }
    if (message.timeIotaMs !== 0n) {
      if (BigInt.asIntN(64, message.timeIotaMs) !== message.timeIotaMs) {
        throw new globalThis.Error("value provided for field message.timeIotaMs of type sint64 too large");
      }
      writer.uint32(40).sint64(message.timeIotaMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BlockParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBlockParams();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.maxTxBytes = reader.sint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.maxDataBytes = reader.sint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.maxBlockBytes = reader.sint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.maxGas = reader.sint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.timeIotaMs = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BlockParams {
    return {
      maxTxBytes: isSet(object.MaxTxBytes)
        ? BigInt(object.MaxTxBytes)
        : isSet(object.max_tx_bytes)
        ? BigInt(object.max_tx_bytes)
        : 0n,
      maxDataBytes: isSet(object.MaxDataBytes)
        ? BigInt(object.MaxDataBytes)
        : isSet(object.max_data_bytes)
        ? BigInt(object.max_data_bytes)
        : 0n,
      maxBlockBytes: isSet(object.MaxBlockBytes)
        ? BigInt(object.MaxBlockBytes)
        : isSet(object.max_block_bytes)
        ? BigInt(object.max_block_bytes)
        : 0n,
      maxGas: isSet(object.MaxGas) ? BigInt(object.MaxGas) : isSet(object.max_gas) ? BigInt(object.max_gas) : 0n,
      timeIotaMs: isSet(object.TimeIotaMS)
        ? BigInt(object.TimeIotaMS)
        : isSet(object.time_iota_ms)
        ? BigInt(object.time_iota_ms)
        : 0n,
    };
  },

  toJSON(message: BlockParams): unknown {
    const obj: any = {};
    if (message.maxTxBytes !== undefined) {
      obj.MaxTxBytes = message.maxTxBytes.toString();
    }
    if (message.maxDataBytes !== undefined) {
      obj.MaxDataBytes = message.maxDataBytes.toString();
    }
    if (message.maxBlockBytes !== undefined) {
      obj.MaxBlockBytes = message.maxBlockBytes.toString();
    }
    if (message.maxGas !== undefined) {
      obj.MaxGas = message.maxGas.toString();
    }
    if (message.timeIotaMs !== undefined) {
      obj.TimeIotaMS = message.timeIotaMs.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlockParams>, I>>(base?: I): BlockParams {
    return BlockParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlockParams>, I>>(object: I): BlockParams {
    const message = createBaseBlockParams();
    message.maxTxBytes = (object.maxTxBytes !== undefined && object.maxTxBytes !== null)
      ? BigInt(object.maxTxBytes)
      : 0n;
    message.maxDataBytes = (object.maxDataBytes !== undefined && object.maxDataBytes !== null)
      ? BigInt(object.maxDataBytes)
      : 0n;
    message.maxBlockBytes = (object.maxBlockBytes !== undefined && object.maxBlockBytes !== null)
      ? BigInt(object.maxBlockBytes)
      : 0n;
    message.maxGas = (object.maxGas !== undefined && object.maxGas !== null) ? BigInt(object.maxGas) : 0n;
    message.timeIotaMs = (object.timeIotaMs !== undefined && object.timeIotaMs !== null)
      ? BigInt(object.timeIotaMs)
      : 0n;
    return message;
  },
};

function createBaseValidatorParams(): ValidatorParams {
  return { pubKeyTypeUrLs: [] };
}

export const ValidatorParams: MessageFns<ValidatorParams> = {
  encode(message: ValidatorParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.pubKeyTypeUrLs) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidatorParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseValidatorParams();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.pubKeyTypeUrLs.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ValidatorParams {
    return {
      pubKeyTypeUrLs: globalThis.Array.isArray(object?.PubKeyTypeURLs)
        ? object.PubKeyTypeURLs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.pub_key_type_ur_ls)
        ? object.pub_key_type_ur_ls.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ValidatorParams): unknown {
    const obj: any = {};
    if (message.pubKeyTypeUrLs?.length) {
      obj.PubKeyTypeURLs = message.pubKeyTypeUrLs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidatorParams>, I>>(base?: I): ValidatorParams {
    return ValidatorParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidatorParams>, I>>(object: I): ValidatorParams {
    const message = createBaseValidatorParams();
    message.pubKeyTypeUrLs = object.pubKeyTypeUrLs?.map((e) => e) || [];
    return message;
  },
};

function createBaseValidatorUpdate(): ValidatorUpdate {
  return { address: "", pubKey: undefined, power: 0n };
}

export const ValidatorUpdate: MessageFns<ValidatorUpdate> = {
  encode(message: ValidatorUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.pubKey !== undefined) {
      Any.encode(message.pubKey, writer.uint32(18).fork()).join();
    }
    if (message.power !== 0n) {
      if (BigInt.asIntN(64, message.power) !== message.power) {
        throw new globalThis.Error("value provided for field message.power of type sint64 too large");
      }
      writer.uint32(24).sint64(message.power);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidatorUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseValidatorUpdate();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.address = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.pubKey = Any.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.power = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ValidatorUpdate {
    return {
      address: isSet(object.Address)
        ? globalThis.String(object.Address)
        : isSet(object.address)
        ? globalThis.String(object.address)
        : "",
      pubKey: isSet(object.PubKey)
        ? Any.fromJSON(object.PubKey)
        : isSet(object.pub_key)
        ? Any.fromJSON(object.pub_key)
        : undefined,
      power: isSet(object.Power) ? BigInt(object.Power) : isSet(object.power) ? BigInt(object.power) : 0n,
    };
  },

  toJSON(message: ValidatorUpdate): unknown {
    const obj: any = {};
    if (message.address !== undefined) {
      obj.Address = message.address;
    }
    if (message.pubKey !== undefined) {
      obj.PubKey = Any.toJSON(message.pubKey);
    }
    if (message.power !== undefined) {
      obj.Power = message.power.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidatorUpdate>, I>>(base?: I): ValidatorUpdate {
    return ValidatorUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidatorUpdate>, I>>(object: I): ValidatorUpdate {
    const message = createBaseValidatorUpdate();
    message.address = object.address ?? "";
    message.pubKey = (object.pubKey !== undefined && object.pubKey !== null)
      ? Any.fromPartial(object.pubKey)
      : undefined;
    message.power = (object.power !== undefined && object.power !== null) ? BigInt(object.power) : 0n;
    return message;
  },
};

function createBaseLastCommitInfo(): LastCommitInfo {
  return { round: 0, votes: [] };
}

export const LastCommitInfo: MessageFns<LastCommitInfo> = {
  encode(message: LastCommitInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.round !== 0) {
      writer.uint32(8).sint32(message.round);
    }
    for (const v of message.votes) {
      VoteInfo.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LastCommitInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLastCommitInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.round = reader.sint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.votes.push(VoteInfo.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): LastCommitInfo {
    return {
      round: isSet(object.Round)
        ? globalThis.Number(object.Round)
        : isSet(object.round)
        ? globalThis.Number(object.round)
        : 0,
      votes: globalThis.Array.isArray(object?.Votes)
        ? object.Votes.map((e: any) => VoteInfo.fromJSON(e))
        : globalThis.Array.isArray(object?.votes)
        ? object.votes.map((e: any) => VoteInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: LastCommitInfo): unknown {
    const obj: any = {};
    if (message.round !== undefined) {
      obj.Round = Math.round(message.round);
    }
    if (message.votes?.length) {
      obj.Votes = message.votes.map((e) => VoteInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LastCommitInfo>, I>>(base?: I): LastCommitInfo {
    return LastCommitInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LastCommitInfo>, I>>(object: I): LastCommitInfo {
    const message = createBaseLastCommitInfo();
    message.round = object.round ?? 0;
    message.votes = object.votes?.map((e) => VoteInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVoteInfo(): VoteInfo {
  return { address: "", power: 0n, signedLastBlock: false };
}

export const VoteInfo: MessageFns<VoteInfo> = {
  encode(message: VoteInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.power !== 0n) {
      if (BigInt.asIntN(64, message.power) !== message.power) {
        throw new globalThis.Error("value provided for field message.power of type sint64 too large");
      }
      writer.uint32(16).sint64(message.power);
    }
    if (message.signedLastBlock !== false) {
      writer.uint32(24).bool(message.signedLastBlock);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoteInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVoteInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.address = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.power = reader.sint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.signedLastBlock = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): VoteInfo {
    return {
      address: isSet(object.Address)
        ? globalThis.String(object.Address)
        : isSet(object.address)
        ? globalThis.String(object.address)
        : "",
      power: isSet(object.Power) ? BigInt(object.Power) : isSet(object.power) ? BigInt(object.power) : 0n,
      signedLastBlock: isSet(object.SignedLastBlock)
        ? globalThis.Boolean(object.SignedLastBlock)
        : isSet(object.signed_last_block)
        ? globalThis.Boolean(object.signed_last_block)
        : false,
    };
  },

  toJSON(message: VoteInfo): unknown {
    const obj: any = {};
    if (message.address !== undefined) {
      obj.Address = message.address;
    }
    if (message.power !== undefined) {
      obj.Power = message.power.toString();
    }
    if (message.signedLastBlock !== undefined) {
      obj.SignedLastBlock = message.signedLastBlock;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoteInfo>, I>>(base?: I): VoteInfo {
    return VoteInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoteInfo>, I>>(object: I): VoteInfo {
    const message = createBaseVoteInfo();
    message.address = object.address ?? "";
    message.power = (object.power !== undefined && object.power !== null) ? BigInt(object.power) : 0n;
    message.signedLastBlock = object.signedLastBlock ?? false;
    return message;
  },
};

function createBaseEventString(): EventString {
  return { value: "" };
}

export const EventString: MessageFns<EventString> = {
  encode(message: EventString, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventString {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventString();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.value = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EventString {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: EventString): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventString>, I>>(base?: I): EventString {
    return EventString.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventString>, I>>(object: I): EventString {
    const message = createBaseEventString();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMockHeader(): MockHeader {
  return { version: "", chainId: "", height: 0n, time: undefined, numTxs: 0n, totalTxs: 0n };
}

export const MockHeader: MessageFns<MockHeader> = {
  encode(message: MockHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.chainId !== "") {
      writer.uint32(18).string(message.chainId);
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type sint64 too large");
      }
      writer.uint32(24).sint64(message.height);
    }
    if (message.time !== undefined) {
      Timestamp.encode(message.time, writer.uint32(34).fork()).join();
    }
    if (message.numTxs !== 0n) {
      if (BigInt.asIntN(64, message.numTxs) !== message.numTxs) {
        throw new globalThis.Error("value provided for field message.numTxs of type sint64 too large");
      }
      writer.uint32(40).sint64(message.numTxs);
    }
    if (message.totalTxs !== 0n) {
      if (BigInt.asIntN(64, message.totalTxs) !== message.totalTxs) {
        throw new globalThis.Error("value provided for field message.totalTxs of type sint64 too large");
      }
      writer.uint32(48).sint64(message.totalTxs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MockHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMockHeader();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.version = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.chainId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.height = reader.sint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.time = Timestamp.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.numTxs = reader.sint64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.totalTxs = reader.sint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MockHeader {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      chainId: isSet(object.chainId)
        ? globalThis.String(object.chainId)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      height: isSet(object.height) ? BigInt(object.height) : 0n,
      time: isSet(object.time) ? fromJsonTimestamp(object.time) : undefined,
      numTxs: isSet(object.numTxs) ? BigInt(object.numTxs) : isSet(object.num_txs) ? BigInt(object.num_txs) : 0n,
      totalTxs: isSet(object.totalTxs)
        ? BigInt(object.totalTxs)
        : isSet(object.total_txs)
        ? BigInt(object.total_txs)
        : 0n,
    };
  },

  toJSON(message: MockHeader): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = message.version;
    }
    if (message.chainId !== undefined) {
      obj.chainId = message.chainId;
    }
    if (message.height !== undefined) {
      obj.height = message.height.toString();
    }
    if (message.time !== undefined) {
      obj.time = fromTimestamp(message.time).toISOString();
    }
    if (message.numTxs !== undefined) {
      obj.numTxs = message.numTxs.toString();
    }
    if (message.totalTxs !== undefined) {
      obj.totalTxs = message.totalTxs.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MockHeader>, I>>(base?: I): MockHeader {
    return MockHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MockHeader>, I>>(object: I): MockHeader {
    const message = createBaseMockHeader();
    message.version = object.version ?? "";
    message.chainId = object.chainId ?? "";
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    message.time = (object.time !== undefined && object.time !== null) ? Timestamp.fromPartial(object.time) : undefined;
    message.numTxs = (object.numTxs !== undefined && object.numTxs !== null) ? BigInt(object.numTxs) : 0n;
    message.totalTxs = (object.totalTxs !== undefined && object.totalTxs !== null) ? BigInt(object.totalTxs) : 0n;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Timestamp {
  if (o instanceof globalThis.Date) {
    return toTimestamp(o);
  } else if (typeof o === "string") {
    return toTimestamp(new globalThis.Date(o));
  } else {
    return Timestamp.fromJSON(o);
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
